---
phase: 07.7-hybrid-intent-classifier-few-shot-prompts
plan: 03
subsystem: prompts
tags: [few-shot, directives, token-budget, prompt-tier]

# Dependency graph
requires:
  - phase: 07.7-01
    provides: "Tool.args_schema property on all 37+ tools"
provides:
  - "FEW_SHOT sections in all three directives (supervisor, executor, evaluator)"
  - "COMPACT sections in executor.md and evaluator.md"
  - "_read_directive_section() reusable helper"
  - "_estimate_prompt_tokens() token estimation"
  - "_ROLE_TOKEN_BUDGETS per-role token budget enforcement"
  - "Few-shot injection on full tier, omitted on compact tier"
affects: [07.7-04]

# Tech tracking
tech-stack:
  added: []
  patterns: ["_read_directive_section() for section extraction from directive markdown files", "per-role token budget with cascading truncation"]

key-files:
  created: []
  modified:
    - src/agentic_workflows/directives/supervisor.md
    - src/agentic_workflows/directives/executor.md
    - src/agentic_workflows/directives/evaluator.md
    - src/agentic_workflows/orchestration/langgraph/graph.py
    - tests/unit/test_prompt_tier.py

key-decisions:
  - "Planner token budget set to 2500 (not 1000) — full prompt with tool descriptions already ~1625 tokens; 2500 accommodates normal prompt+few-shot while triggering truncation for bloated prompts"
  - "Few-shot injected on full tier only via ## Examples block; compact tier omits to save context"
  - "Token budget enforcement: truncate tool descriptions first, drop few-shot second"
  - "Fixed directive path resolution from parents[3] to parents[2] — old code silently fell back to hardcoded string via OSError catch"

patterns-established:
  - "_read_directive_section(name, section): generic extractor for ## sections from directive .md files"
  - "Cascading token budget truncation: tool descriptions -> few-shot -> log warning"

requirements-completed: [SC-2, SC-3]

# Metrics
duration: 6min
completed: 2026-03-10
---

# Phase 07.7 Plan 03: Few-Shot Prompts and Token Budgets Summary

**Few-shot JSON examples in all three directives with per-role token budget enforcement and reusable _read_directive_section helper**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-10T13:49:40Z
- **Completed:** 2026-03-10T13:56:08Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Added ## FEW_SHOT sections to supervisor.md (3 examples: sort+write, store-then-query, multi-step analysis), executor.md (2 examples), and evaluator.md (2 examples)
- Added ## COMPACT sections to executor.md and evaluator.md matching supervisor.md pattern
- Extracted `_read_directive_section()` helper replacing inline section-reading code in `_build_system_prompt()`
- Fixed directive path resolution bug (parents[3] -> parents[2]) that caused silent OSError fallback
- Added `_estimate_prompt_tokens()` and `_ROLE_TOKEN_BUDGETS` for per-role token budget enforcement
- Full tier prompt now includes few-shot examples via `## Examples` block; compact tier omits them
- Token budget enforcement truncates tool descriptions first, then drops few-shot if still over budget
- Added 17 new tests (25 total in test_prompt_tier.py), all passing

## Task Commits

Each task was committed atomically:

1. **Task 1: Add FEW_SHOT sections to directives + COMPACT for executor/evaluator** - `0f12c86` (test)
2. **Task 2: _read_directive_section helper + FEW_SHOT injection + token budgets** - `60e96ad` (feat)

## Files Created/Modified
- `src/agentic_workflows/directives/supervisor.md` - Added ## FEW_SHOT with 3 JSON examples
- `src/agentic_workflows/directives/executor.md` - Added ## COMPACT + ## FEW_SHOT with 2 examples
- `src/agentic_workflows/directives/evaluator.md` - Added ## COMPACT + ## FEW_SHOT with 2 examples
- `src/agentic_workflows/orchestration/langgraph/graph.py` - Added _read_directive_section, _estimate_prompt_tokens, _ROLE_TOKEN_BUDGETS; refactored _build_system_prompt
- `tests/unit/test_prompt_tier.py` - Added 17 tests for sections, token estimation, few-shot injection, budgets

## Decisions Made
- Planner budget adjusted from plan's 1000 to 2500 tokens: the existing full prompt is ~1625 tokens before any few-shot, so 1000 would truncate even the base prompt
- Few-shot examples follow the existing JSON action format used in the system prompt
- Directive path fixed from parents[3] to parents[2] (Rule 1 bug fix — old code silently used fallback)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed directive path resolution**
- **Found during:** Task 2
- **Issue:** `Path(__file__).resolve().parents[3]` resolves to `src/` not `src/agentic_workflows/`, causing OSError when reading directives. Old code masked this with fallback string.
- **Fix:** Changed to `parents[2]` which correctly resolves to `src/agentic_workflows/`
- **Files modified:** `src/agentic_workflows/orchestration/langgraph/graph.py`
- **Commit:** `60e96ad`

**2. [Rule 2 - Missing functionality] Adjusted planner token budget**
- **Found during:** Task 2
- **Issue:** Plan specified 1000-token budget but full prompt is ~1625 tokens baseline, causing every prompt to truncate
- **Fix:** Set budget to 2500 tokens to accommodate normal prompt + few-shot while still triggering on bloated prompts
- **Commit:** `60e96ad`

## Issues Encountered

None beyond the deviations above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All directives have FEW_SHOT sections ready for model guidance
- _read_directive_section() available for Plan 04 (format correction escalation) to read directive sections
- Token budget infrastructure in place for future per-role budget tuning

---
*Phase: 07.7-hybrid-intent-classifier-few-shot-prompts*
*Completed: 2026-03-10*

## Self-Check: PASSED
