---
phase: 07.2-architecture-review-implementation-critical-bug-fixes-and-systemic-hardening
plan: "02"
subsystem: orchestration
tags: [sqlite, wal, checkpoint, serialization, set, eviction, performance]

# Dependency graph
requires:
  - phase: 07.2-01
    provides: "Wave 1 dual-execution and callback isolation fixes"
provides:
  - "SQLiteCheckpointStore persistent connection with WAL mode"
  - "seen_tool_signatures as O(1) set instead of O(n) list"
  - "Bounded pipeline_trace (500), handoff_queue (50), handoff_results (50)"
affects: [07.2-03, 07.2-04, orchestration, checkpoint-store]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Persistent SQLite connection with threading.Lock (matches SQLiteRunStore)"
    - "Set-aware JSON serialization via sorted() default handler"
    - "Oldest-first eviction on capped lists"

key-files:
  created: []
  modified:
    - "src/agentic_workflows/orchestration/langgraph/checkpoint_store.py"
    - "src/agentic_workflows/orchestration/langgraph/state_schema.py"
    - "src/agentic_workflows/orchestration/langgraph/graph.py"
    - "src/agentic_workflows/orchestration/langgraph/context_manager.py"
    - "tests/unit/test_state_schema.py"
    - "tests/unit/test_run_helpers.py"

key-decisions:
  - "Persistent connection + WAL mode for checkpoint store — matches SQLiteRunStore pattern from storage/sqlite.py"
  - "set[str] for seen_tool_signatures — O(1) membership checks instead of O(n) list scans"
  - "Local _PIPELINE_TRACE_CAP in context_manager.py — avoids circular import with graph.py"
  - "Set serialization via sorted() in json.dumps default handler — deterministic JSON output"

patterns-established:
  - "Cap constants at module level with oldest-first eviction after every append"
  - "List-to-set conversion in ensure_state_defaults for checkpoint deserialization safety"

requirements-completed: []

# Metrics
duration: 6min
completed: 2026-03-08
---

# Phase 7.2 Plan 02: Wave 2 Critical Bottleneck Fixes Summary

**SQLiteCheckpointStore persistent WAL connection, seen_tool_signatures O(1) set conversion, and bounded list caps for pipeline_trace/handoff queues**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-08T00:53:42Z
- **Completed:** 2026-03-08T00:59:51Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments
- SQLiteCheckpointStore now uses a single persistent connection with WAL journal mode and threading.Lock, eliminating per-save connection overhead
- seen_tool_signatures converted from Annotated[list[str], operator.add] to plain set[str] for O(1) membership checks; all read/write sites updated (.add instead of .append, set() defaults)
- pipeline_trace capped at 500 entries, handoff_queue and handoff_results capped at 50 entries each, with oldest-first eviction preventing unbounded memory growth
- 8 new tests added (662 total passing, up from 658)

## Task Commits

Each task was committed atomically:

1. **Task 1: SQLiteCheckpointStore WAL + persistent connection (W2-3)** - `d70b1a2` (feat)
2. **Task 2: Convert seen_tool_signatures to set + fix serialization (W2-4)** - `02a96c0` (feat)
3. **Task 3: Cap pipeline_trace, handoff_queue, handoff_results (W2-5)** - `55e1922` (feat)

## Files Created/Modified
- `src/agentic_workflows/orchestration/langgraph/checkpoint_store.py` - Persistent _conn + _lock + WAL mode; removed _connect(); set-aware JSON serializer
- `src/agentic_workflows/orchestration/langgraph/state_schema.py` - seen_tool_signatures: set[str]; set() initialization; list-to-set conversion in ensure_state_defaults
- `src/agentic_workflows/orchestration/langgraph/graph.py` - Removed seen_tool_signatures from _ANNOTATED_LIST_FIELDS; .add() instead of .append(); cap constants + eviction logic
- `src/agentic_workflows/orchestration/langgraph/context_manager.py` - _PIPELINE_TRACE_CAP constant + eviction in _emit_eviction_event
- `tests/unit/test_state_schema.py` - 4 new tests: persistent connection, set type assertion, checkpoint roundtrip, ensure_state_defaults conversion
- `tests/unit/test_run_helpers.py` - 4 new tests: pipeline_trace cap, below-cap passthrough, handoff_queue cap, handoff_results cap

## Decisions Made
- Used persistent connection + WAL mode pattern from SQLiteRunStore (storage/sqlite.py) for consistency across the codebase
- Defined _PIPELINE_TRACE_CAP locally in context_manager.py (value 500) to avoid circular import with graph.py
- Used sorted() in json.dumps default handler for deterministic set serialization (not repr() which produces non-JSON output)
- Added list-to-set conversion in ensure_state_defaults so checkpointed state (deserialized from JSON as list) gets properly restored to set

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Wave 2 bottlenecks resolved; pipeline_trace, handoff_queue, and handoff_results are now bounded
- Checkpoint store uses efficient persistent connection pattern
- seen_tool_signatures provides O(1) dedup checks
- Ready for Wave 3 (Plan 03): test harness hardening and remaining fixes

---
*Phase: 07.2-architecture-review-implementation-critical-bug-fixes-and-systemic-hardening*
*Completed: 2026-03-08*
