---
phase: 07.2-architecture-review-implementation-critical-bug-fixes-and-systemic-hardening
plan: "04"
subsystem: testing, security
tags: [run_bash, memoize, tool-contracts, env-guard, parametrized-tests]

# Dependency graph
requires:
  - phase: 07.2-03
    provides: auto-derived annotated fields, prepare_state single source of truth
  - phase: 07.2-00
    provides: test_tool_contracts.py scaffold with 144 parametrized stubs
provides:
  - P1_BASH_ENABLED opt-in guard on run_bash (disabled by default)
  - Cleaner planner system prompt (memoize removed from tool list, automatic memoization policy)
  - 144 passing parametrized tool contract tests (4 contracts x 36 tools)
affects: [run.py, user_run.py, any code that calls run_bash tool]

# Tech tracking
tech-stack:
  added: []
  patterns: [env-var-gated-off-by-default security pattern, parametrized contract testing]

key-files:
  created: []
  modified:
    - src/agentic_workflows/tools/run_bash.py
    - src/agentic_workflows/orchestration/langgraph/graph.py
    - tests/unit/test_run_bash.py
    - tests/unit/test_run_helpers.py
    - tests/unit/test_tool_contracts.py
    - tests/unit/test_tool_security.py

key-decisions:
  - "P1_BASH_ENABLED guard uses != 'true' pattern (ruff SIM201 compliant) at top of execute()"
  - "Memoize tool kept in registry for internal auto-memoize use, only removed from planner prompt"
  - "Tool contract test accepts KeyError/ValueError/TypeError as valid responses to empty args"

patterns-established:
  - "Env-var-gated-off-by-default: security-sensitive tools disabled unless explicitly opted in via env var"
  - "Parametrized contract testing: 4 invariants tested across all registered tools via build_tool_registry"

requirements-completed: []

# Metrics
duration: 4min
completed: 2026-03-08
---

# Phase 07.2 Plan 04: Final Wave 3 Improvements Summary

**P1_BASH_ENABLED security guard, memoize removal from planner prompt, and 144 parametrized tool contract tests**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-08T01:10:53Z
- **Completed:** 2026-03-08T01:14:48Z
- **Tasks:** 4
- **Files modified:** 6

## Accomplishments
- run_bash tool now disabled by default; requires P1_BASH_ENABLED=true env var to execute commands
- Planner system prompt no longer lists memoize as a callable tool; memoization policy updated to "automatic"
- All 144 parametrized tool contract stubs (from plan 00 scaffold) replaced with real assertions -- all pass
- Full test suite: 823 tests passing (up from 672 + 144 stubs), zero failures, ruff clean

## Task Commits

Each task was committed atomically:

1. **Task 1: P1_BASH_ENABLED guard (W3-8)** - `c178ab5` (test: RED), `0826a8a` (feat: GREEN)
2. **Task 2: Remove memoize from planner prompt (W3-9)** - `0fb5ce2` (test: RED), `6868271` (feat: GREEN)
3. **Task 3: Parametrized tool contract tests (W3-10)** - `f57595f` (feat: replace stubs with assertions)
4. **Task 4: Full suite regression gate** - `e3f3214` (fix: test_tool_security + ruff SIM201)

_Note: TDD tasks have two commits each (RED test, GREEN implementation)_

## Files Created/Modified
- `src/agentic_workflows/tools/run_bash.py` - Added P1_BASH_ENABLED opt-in guard at top of execute()
- `src/agentic_workflows/orchestration/langgraph/graph.py` - Removed memoize from tool arg reference in _build_system_prompt(); updated Memoization policy
- `tests/unit/test_run_bash.py` - Added test_run_bash_disabled_by_default; updated all tests with monkeypatch.setenv
- `tests/unit/test_run_helpers.py` - Added TestSystemPromptMemoizeRemoval class (3 tests)
- `tests/unit/test_tool_contracts.py` - Replaced 4 pytest.fail stubs with real assertions + error handling
- `tests/unit/test_tool_security.py` - Added P1_BASH_ENABLED=true to RunBash security tests

## Decisions Made
- P1_BASH_ENABLED guard uses `!= "true"` pattern (ruff SIM201 compliant) at top of execute()
- Memoize tool kept in registry for internal auto-memoize use; only removed from planner prompt's tool arg reference
- Tool contract test_tool_execute_returns_dict_on_empty_args accepts KeyError, ValueError, TypeError, and AgenticWorkflowError as valid responses (some tools require specific args)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed test_tool_security tests broken by P1_BASH_ENABLED guard**
- **Found during:** Task 4 (Full suite regression gate)
- **Issue:** TestRunBashToolSecurity.test_blocks_denied_command and test_allows_when_env_unset failed because run_bash now requires P1_BASH_ENABLED=true
- **Fix:** Added monkeypatch.setenv("P1_BASH_ENABLED", "true") to both tests
- **Files modified:** tests/unit/test_tool_security.py
- **Verification:** Full suite 823 passed
- **Committed in:** e3f3214

**2. [Rule 1 - Bug] Fixed ruff SIM201 lint warning in run_bash.py**
- **Found during:** Task 4 (ruff check)
- **Issue:** `not ... == "true"` pattern flagged as SIM201; should use `!= "true"`
- **Fix:** Changed to `os.environ.get("P1_BASH_ENABLED", "").strip().lower() != "true"`
- **Files modified:** src/agentic_workflows/tools/run_bash.py
- **Verification:** ruff check clean on all modified files
- **Committed in:** e3f3214

---

**Total deviations:** 2 auto-fixed (2 Rule 1 bugs)
**Impact on plan:** Both auto-fixes necessary for correctness. No scope creep.

## Issues Encountered
None beyond the auto-fixed deviations above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 7.2 Plan 05 is the final plan (if any); all Wave 3 improvements complete
- 823 tests passing; 0 failures; ruff clean
- run_bash now requires P1_BASH_ENABLED=true in .env for live usage (run.py, user_run.py)

---
*Phase: 07.2-architecture-review-implementation-critical-bug-fixes-and-systemic-hardening*
*Completed: 2026-03-08*

## Self-Check: PASSED

- All 6 modified files verified present on disk
- All 6 commits verified in git log (c178ab5, 0826a8a, 0fb5ce2, 6868271, f57595f, e3f3214)
- SUMMARY.md created at expected path
