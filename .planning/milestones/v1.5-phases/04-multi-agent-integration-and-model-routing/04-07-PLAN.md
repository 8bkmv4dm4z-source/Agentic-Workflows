---
phase: 04-multi-agent-integration-and-model-routing
plan: 07
type: retrospective
wave: 1
depends_on: [04-06]
files_modified:
  - src/agentic_workflows/tools/parse_code_structure.py
  - src/agentic_workflows/tools/describe_db_schema.py
  - src/agentic_workflows/tools/tools_registry.py
  - src/agentic_workflows/orchestration/langgraph/graph.py
  - src/agentic_workflows/orchestration/langgraph/mission_parser.py
  - src/agentic_workflows/orchestration/langgraph/run.py
  - tests/unit/test_parse_code_structure.py
  - tests/unit/test_describe_db_schema.py
autonomous: true
gap_closure: false
requirements: []

must_haves:
  truths:
    - "372 tests pass (21 new: 13 for parse_code_structure, 8 for describe_db_schema)"
    - "ruff check src/ tests/ is clean"
    - "clarify action node wired: graph has clarify_node → finalize edge"
    - "_diagnose_incomplete_missions() injected into finish rejection messages on both paths"
    - "UI panels render correctly: clarification box, context warning, stuck indicator"
  artifacts:
    - path: "src/agentic_workflows/tools/parse_code_structure.py"
      provides: "AST + regex code structure analyzer"
      contains: "parse_code_structure"
    - path: "src/agentic_workflows/tools/describe_db_schema.py"
      provides: "SQLite PRAGMA + CSV reader schema describer"
      contains: "describe_db_schema"
  key_links:
    - from: "graph.py::clarify_node"
      to: "finalize"
      via: "direct edge added after clarify action dispatch"
      pattern: "clarify"
---

<objective>
Retrospective plan documenting 5 improvements added to the codebase during Phase 04 outside the formal GSD cycle. All work is already implemented and verified (372 tests passing, ruff clean).

Changes:
1. `parse_code_structure` tool — AST + regex code structure analyzer, 13 unit tests
2. `describe_db_schema` tool — SQLite PRAGMA + CSV reader, 8 unit tests
3. `clarify` action type — planner can break confusion loops, wired in graph with clarify_node → finalize edge
4. `_diagnose_incomplete_missions()` — injected into finish rejection messages on both rejection paths
5. UI panels — clarification box, context warning, stuck indicator added to run.py

Purpose: Close the gap between codebase state and planning documentation.
Output: No code changes — all implementation is already done and verified.
</objective>

<tasks>

<task type="retrospective">
  <name>Task 1: parse_code_structure tool</name>
  <files>
    src/agentic_workflows/tools/parse_code_structure.py
    src/agentic_workflows/tools/tools_registry.py
    tests/unit/test_parse_code_structure.py
  </files>
  <behavior>
    - Accepts a file path or code string and extracts structural elements (functions, classes, imports)
    - Uses Python AST for .py files; regex fallback for other languages
    - Returns structured dict with "functions", "classes", "imports", "language" keys
    - Registered in tools_registry.py as "parse_code_structure"
    - 13 unit tests in tests/unit/test_parse_code_structure.py — all passing
  </behavior>
  <done>parse_code_structure tool implemented, registered, and tested. 13 tests pass.</done>
</task>

<task type="retrospective">
  <name>Task 2: describe_db_schema tool</name>
  <files>
    src/agentic_workflows/tools/describe_db_schema.py
    src/agentic_workflows/tools/tools_registry.py
    tests/unit/test_describe_db_schema.py
  </files>
  <behavior>
    - Accepts a SQLite database path or a CSV file path
    - For SQLite: uses PRAGMA table_info() and sqlite_master to list tables and columns
    - For CSV: reads header row and infers column types from first N rows
    - Returns structured dict with "tables" (SQLite) or "columns" (CSV) metadata
    - Registered in tools_registry.py as "describe_db_schema"
    - 8 unit tests in tests/unit/test_describe_db_schema.py — all passing
  </behavior>
  <done>describe_db_schema tool implemented, registered, and tested. 8 tests pass.</done>
</task>

<task type="retrospective">
  <name>Task 3: clarify action type</name>
  <files>
    src/agentic_workflows/orchestration/langgraph/graph.py
    src/agentic_workflows/schemas.py
  </files>
  <behavior>
    - New action type "clarify" allows the planner to emit a clarification request when it detects a confusion loop
    - clarify_node added to graph.py — extracts clarification message from action, stores in state, routes to finalize
    - Wired as: plan_node → clarify_node (on clarify action) → finalize
    - Prevents infinite loops where planner re-emits identical failed actions
    - schemas.py updated to recognize "clarify" as a valid action type
  </behavior>
  <done>clarify action type wired in graph. clarify_node → finalize edge established.</done>
</task>

<task type="retrospective">
  <name>Task 4: _diagnose_incomplete_missions() injection</name>
  <files>
    src/agentic_workflows/orchestration/langgraph/graph.py
  </files>
  <behavior>
    - New helper _diagnose_incomplete_missions(state) inspects incomplete missions and returns a human-readable diagnosis string
    - Injected into both finish-rejection paths in graph.py (the "finish rejected" message that loops back to planning)
    - Diagnosis identifies which missions are incomplete, which tools are missing, and suggests recovery actions
    - Helps planner recover from stuck states by providing structured context in the rejection message
  </behavior>
  <done>_diagnose_incomplete_missions() added and injected into both finish rejection message paths.</done>
</task>

<task type="retrospective">
  <name>Task 5: UI panels in run.py</name>
  <files>
    src/agentic_workflows/orchestration/langgraph/run.py
  </files>
  <behavior>
    - render_clarification_panel(state): displays clarification request box when state contains a clarification message
    - render_context_warning_panel(state): displays warning when context (token budget) is below threshold
    - render_stuck_indicator(state): displays stuck indicator when repeated identical actions are detected
    - Panels integrated into the CLI output loop in run.py
    - Rich-formatted output with clear visual separation from normal execution logs
  </behavior>
  <done>Three UI panels added to run.py CLI output. Render correctly during demo execution.</done>
</task>

</tasks>

<verification>
All work already verified:

```bash
# Full test suite — 372 passed
python3 -m pytest tests/ -q

# Lint — 0 errors
python3 -m ruff check src/

# New tool tests specifically
python3 -m pytest tests/unit/test_parse_code_structure.py tests/unit/test_describe_db_schema.py -v
```

Expected: 372 passed, 0 failed. ruff clean. 21 new tests (13 + 8) passing.
</verification>

<success_criteria>
- parse_code_structure tool: 13 unit tests pass, registered in tools_registry
- describe_db_schema tool: 8 unit tests pass, registered in tools_registry
- clarify action: clarify_node → finalize edge wired in graph.py
- _diagnose_incomplete_missions(): injected into both finish rejection paths
- UI panels: render_clarification_panel, render_context_warning_panel, render_stuck_indicator present in run.py
- Total: 372 tests passing, ruff clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-agent-integration-and-model-routing/04-07-SUMMARY.md`
</output>
