---
phase: 07.6
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_provider_context_size.py
  - tests/unit/test_prompt_tier.py
  - tests/unit/test_cursor_persistence.py
  - tests/unit/test_structural_health.py
autonomous: true
requirements:
  - structural hardening, no external requirement IDs
must_haves:
  truths:
    - "All 4 new test files exist and are importable (no ImportError at collection)"
    - "Each file contains at least one test that FAILS (NotImplementedError or ImportError) confirming RED state"
    - "Existing 823+ tests are unaffected by the new stubs"
  artifacts:
    - path: "tests/unit/test_provider_context_size.py"
      provides: "Stub tests for context_size() on all providers + ScriptedProvider"
      min_lines: 20
    - path: "tests/unit/test_prompt_tier.py"
      provides: "Stub tests for _select_prompt_tier(), compact prompt, COMPACT directive, env block"
      min_lines: 25
    - path: "tests/unit/test_cursor_persistence.py"
      provides: "Stub tests for upsert_cursor, get_cursor, get_active_cursors, eviction hint, bypass"
      min_lines: 30
    - path: "tests/unit/test_structural_health.py"
      provides: "Stub tests for structural_health in audit_report, fallback counter increment"
      min_lines: 15
  key_links:
    - from: "tests/unit/test_provider_context_size.py"
      to: "src/agentic_workflows/orchestration/langgraph/provider.py"
      via: "imports ChatProvider, LlamaCppChatProvider, OllamaChatProvider, GroqChatProvider, OpenAIChatProvider"
      pattern: "from agentic_workflows.orchestration.langgraph.provider import"
---

<objective>
Create all Wave 0 failing test stubs required by the validation strategy. Every new test file from VALIDATION.md must exist, be importable, and fail with NotImplementedError so downstream implementation plans have a clear RED baseline.

Purpose: Nyquist compliance requires every new behavior to have a pre-existing test before implementation. Wave 0 creates the test scaffolding; subsequent waves turn them green.
Output: 4 new test files with importable stubs that fail at runtime.
</objective>

<execution_context>
@/home/nir/.claude/get-shit-done/workflows/execute-plan.md
@/home/nir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.6-llm-output-structure-stabilization/07.6-CONTEXT.md
@.planning/phases/07.6-llm-output-structure-stabilization/07.6-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create provider and prompt tier test stubs</name>
  <files>tests/unit/test_provider_context_size.py, tests/unit/test_prompt_tier.py</files>
  <behavior>
    - test_provider_context_size.py:
      - test_llama_cpp_context_size_default() — expects 8192 (env default)
      - test_llama_cpp_context_size_env_override() — monkeypatches LLAMA_CPP_N_CTX=4096, expects 4096
      - test_groq_context_size() — expects 32768
      - test_openai_context_size() — expects 128000
      - test_ollama_context_size_default() — expects 32768 when OLLAMA_NUM_CTX unset and num_ctx==0
      - test_scripted_provider_context_size() — expects 32768 (stays in full tier)
    - test_prompt_tier.py:
      - test_compact_tier_for_small_context() — _select_prompt_tier(8192) == "compact"
      - test_compact_tier_boundary() — _select_prompt_tier(10000) == "compact"
      - test_full_tier_above_boundary() — _select_prompt_tier(10001) == "full"
      - test_full_tier_for_large_context() — _select_prompt_tier(128000) == "full"
      - test_compact_prompt_excludes_arg_signatures() — system prompt in compact tier omits per-tool arg lines
      - test_compact_prompt_includes_env_block() — compact prompt contains "python3 is available"
      - test_full_prompt_includes_env_block() — full prompt contains "python3 is available"
      - test_compact_directive_in_compact_prompt() — compact prompt contains "## COMPACT" section text
  </behavior>
  <action>
    Create tests/unit/test_provider_context_size.py with imports from provider.py and stubs raising NotImplementedError. Add ScriptedProvider import from tests.conftest.

    IMPORTANT — stub pattern: each test body is `raise NotImplementedError("stub — implement in plan 07.6-01")`. This ensures RED state without import errors.

    For provider test stubs, import the four concrete provider classes. LlamaCppChatProvider, OllamaChatProvider, GroqChatProvider, OpenAIChatProvider are all in src/agentic_workflows/orchestration/langgraph/provider.py. ScriptedProvider is in tests/conftest.py.

    For test_prompt_tier.py, import `_select_prompt_tier` from `agentic_workflows.orchestration.langgraph.graph`. Use `pytest.raises(ImportError)` pattern to handle the case that `_select_prompt_tier` does not yet exist — wrap the import in a try/except and call pytest.skip("function not yet defined") to distinguish "stub not implemented" from "import failure". Actually — simpler: import with `importlib` and catch AttributeError. Even simpler: just import at module level inside a try, and if AttributeError, define a sentinel. Keep stubs as NotImplementedError to stay Nyquist-clean.

    LangGraphOrchestrator tests that exercise _build_system_prompt() need ScriptedProvider. Instantiate with `ScriptedProvider(responses=[{"action":"finish","answer":"x"}])` passed to `LangGraphOrchestrator(provider=scripted)`.
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && pytest tests/unit/test_provider_context_size.py tests/unit/test_prompt_tier.py -q 2>&1 | tail -20</automated>
  </verify>
  <done>Both files exist, collect without ImportError, all test functions fail with NotImplementedError (RED state confirmed). pytest tests/unit/ -q exits with failures only from the new stubs, zero regressions in other tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create cursor persistence and structural health test stubs</name>
  <files>tests/unit/test_cursor_persistence.py, tests/unit/test_structural_health.py</files>
  <behavior>
    - test_cursor_persistence.py:
      - test_upsert_and_get_cursor_round_trip() — upsert_cursor() then get_cursor() returns same offset (pool=None path)
      - test_get_cursor_missing_returns_none() — get_cursor() for unknown key returns None
      - test_get_active_cursors_filters_by_run_id() — two cursors with different run_ids, get_active_cursors("run-A") returns only run-A cursor
      - test_upsert_cursor_overwrite() — upsert same key twice, get_cursor returns latest offset
      - test_eviction_hint_injected_after_compact() — ContextManager.compact() injects [Orchestrator] message when active cursor exists
      - test_cursor_resume_bypass_seen_signatures() — action dict with __cursor_resume=True bypasses seen_tool_signatures dedup in graph.py planner
    - test_structural_health.py:
      - test_audit_report_has_structural_health_key() — RunResult audit_report contains "structural_health" key
      - test_json_parse_fallback_counter_increments() — run with malformed JSON increments json_parse_fallback counter
      - test_schema_mismatch_counter_present() — audit_report["structural_health"]["schema_mismatch"] exists and is int
  </behavior>
  <action>
    Create tests/unit/test_cursor_persistence.py importing MissionContextStore from agentic_workflows.storage.mission_context_store. All test bodies raise NotImplementedError("stub — implement in plan 07.6-03").

    Create tests/unit/test_structural_health.py importing LangGraphOrchestrator and ScriptedProvider. All test bodies raise NotImplementedError("stub — implement in plan 07.6-04").

    Use the same stub pattern as Task 1: stubs raise NotImplementedError so the files are importable (no collection errors) but all tests fail (RED state).
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && pytest tests/unit/test_cursor_persistence.py tests/unit/test_structural_health.py -q 2>&1 | tail -20</automated>
  </verify>
  <done>Both files exist, collect without ImportError, all stubs fail with NotImplementedError. Running pytest tests/unit/ -q shows new failures only in stub files — zero regressions in existing 823+ tests.</done>
</task>

</tasks>

<verification>
After both tasks: `pytest tests/unit/ -q 2>&1 | tail -10` — should show 823 tests passing + N new failures from stubs, no collection errors, no new import errors.
</verification>

<success_criteria>
- 4 new test stub files exist in tests/unit/
- All 4 files importable (pytest collects them without ImportError)
- All stub test functions raise NotImplementedError (confirmed RED state)
- Existing 823+ tests unchanged (zero regressions)
- pytest tests/unit/ -q shows only stub failures, no surprises
</success_criteria>

<output>
After completion, create `.planning/phases/07.6-llm-output-structure-stabilization/07.6-00-SUMMARY.md`
</output>
