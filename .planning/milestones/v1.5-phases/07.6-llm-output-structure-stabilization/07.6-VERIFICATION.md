---
phase: 07.6-llm-output-structure-stabilization
verified: 2026-03-09T15:14:40Z
status: passed
score: 16/16 must-haves verified
re_verification: false
---

# Phase 07.6: LLM Output Structure Stabilization Verification Report

**Phase Goal:** Mechanically enforce LLM output structure before multi-agent teams deploy with weaker executor models. Fix phi4 context overflow (8192-token context), add provider-aware compact prompts, re-enable GBNF grammar for llama-cpp, instrument fallback parser, convert handoff TypedDicts to Pydantic, persist chunked-read cursors to survive context eviction, and add structural health metrics to the audit report.
**Verified:** 2026-03-09T15:14:40Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | Wave 0: All 4 test stub files exist and pass pytest collection | VERIFIED | All 4 files exist; 25 tests pass, zero NotImplementedError stubs remain |
| 2  | provider.context_size() returns correct int for all 4 provider classes + ScriptedProvider | VERIFIED | `def context_size` found at lines 71, 200, 243, 295, 542 in provider.py; ScriptedProvider at conftest.py:41 |
| 3  | _select_prompt_tier(8192) returns "compact"; _select_prompt_tier(128000) returns "full" | VERIFIED | `_select_prompt_tier` at graph.py:151; boundary: context_size <= 10000 => compact |
| 4  | LangGraphOrchestrator.__init__ stores self._prompt_tier via provider.context_size() | VERIFIED | graph.py:185 `_ctx_size = self.provider.context_size()`, graph.py:189 `self._prompt_tier = _select_prompt_tier(_ctx_size)` |
| 5  | Compact prompt omits arg signatures, includes COMPACT directive + env block | VERIFIED | supervisor.md:1 has `## COMPACT` section; graph.py:285 has `python3 is available`; compact branch at graph.py:290 |
| 6  | Both tiers include env block ("python3 is available") | VERIFIED | graph.py:285 single env_block injected into both branches |
| 7  | parse_action_json() emits WARNING on fallback; returns (dict, bool) tuple | VERIFIED | action_parser.py:84 signature `-> tuple[dict[str, Any], bool]`; action_parser.py:102 `PARSER FALLBACK used=extract_first_json` |
| 8  | validate_action() / parse_action_json() thread step parameter through | VERIFIED | action_parser.py:29 validate_action returns tuple; step passed to parse_action_json internally |
| 9  | TaskHandoff and HandoffResult are Pydantic BaseModel with extra="forbid" | VERIFIED | handoff.py:15,28 `class TaskHandoff(BaseModel)`, `class HandoffResult(BaseModel)` with ConfigDict(extra="forbid") |
| 10 | graph.py handoff_queue/handoff_results call sites use .model_dump() | VERIFIED | graph.py:1592, 1632 `.model_dump()` at both append sites |
| 11 | test_handoff.py uses attribute access; extra-field rejection test passes | VERIFIED | test_handoff.py:21-22,37,46 use h.task_id / h.specialist; extra-field test at line 49-58; 15 tests pass |
| 12 | MissionContextStore has upsert_cursor/get_cursor/get_active_cursors (pool=None path) | VERIFIED | mission_context_store.py:240, 281, 305 — all three methods present |
| 13 | storage/migrations/005_sub_task_cursors.sql DDL exists | VERIFIED | File exists; `CREATE TABLE IF NOT EXISTS sub_task_cursors` at line 4 |
| 14 | context_manager.py compact() injects [Orchestrator] cursor hint when active cursors exist | VERIFIED | context_manager.py:598 `active = self._store.get_active_cursors(run_id)` inside compact() |
| 15 | __cursor_resume=True + tool_name==read_file_chunk bypasses seen_tool_signatures check | VERIFIED | graph.py:1829 `action.get("__cursor_resume") is True` with narrowly scoped bypass (also checks tool_name==read_file_chunk at line 1830) |
| 16 | RunState.structural_health tracked; audit_report["structural_health"] present after every run | VERIFIED | state_schema.py:118 field; ensure_state_defaults:250-254 init; graph.py _finalize():2221 attaches to audit_report; test_structural_health.py 4 tests green |

**Score:** 16/16 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tests/unit/test_provider_context_size.py` | Stub → green tests for context_size() | VERIFIED | 65 lines; all tests pass |
| `tests/unit/test_prompt_tier.py` | Stub → green tests for _select_prompt_tier() | VERIFIED | 70 lines; all tests pass |
| `tests/unit/test_cursor_persistence.py` | Stub → green tests for cursor methods | VERIFIED | 168 lines; all tests pass |
| `tests/unit/test_structural_health.py` | Stub → green tests for audit_report["structural_health"] | VERIFIED | 110 lines; all tests pass; exercises full run() -> RunResult path |
| `src/agentic_workflows/orchestration/langgraph/provider.py` | context_size() on Protocol + all 4 concrete classes | VERIFIED | 5 occurrences; GBNF grammar also enabled by default (line 538) |
| `src/agentic_workflows/orchestration/langgraph/graph.py` | _select_prompt_tier(), _prompt_tier, json_parse_fallback counter, structural_health copy in _finalize(), upsert_cursor call, __cursor_resume bypass | VERIFIED | All 7 patterns confirmed present |
| `src/agentic_workflows/directives/supervisor.md` | ## COMPACT section at top | VERIFIED | Line 1 is `## COMPACT` |
| `src/agentic_workflows/orchestration/langgraph/action_parser.py` | parse_action_json returns (dict, bool); PARSER FALLBACK WARNING; step parameter | VERIFIED | Lines 84, 102, 107 |
| `src/agentic_workflows/orchestration/langgraph/handoff.py` | TaskHandoff/HandoffResult as Pydantic BaseModel with extra="forbid" | VERIFIED | Lines 15, 28; ConfigDict(extra="forbid") at lines 18, 31 |
| `src/agentic_workflows/storage/mission_context_store.py` | upsert_cursor, get_cursor, get_active_cursors | VERIFIED | Lines 240, 281, 305 |
| `storage/migrations/005_sub_task_cursors.sql` | Postgres DDL for sub_task_cursors | VERIFIED | File present; correct PRIMARY KEY definition |
| `src/agentic_workflows/orchestration/langgraph/context_manager.py` | get_active_cursors() call in compact() | VERIFIED | Line 598 |
| `src/agentic_workflows/orchestration/langgraph/state_schema.py` | structural_health field + ensure_state_defaults init | VERIFIED | Lines 118, 180, 250-254 |
| `src/agentic_workflows/orchestration/langgraph/mission_auditor.py` | structural_health passthrough (plan said contains: "structural_health") | NOTE | Plan 04 task description correctly placed structural_health attachment in graph.py _finalize() after audit.to_dict() — not in auditor itself. This is the intended design; auditor.to_dict() is immutable and graph.py mutates the dict post-call. Functional goal is achieved. |
| `.env.example` | GBNF grammar comment: "grammar is ENABLED by default; set to 0 to disable" | VERIFIED | Line 37: `grammar is ENABLED by default; set to 0 to disable GBNF enforcement` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `graph.py` | `provider.py` | `self.provider.context_size()` in __init__ | WIRED | graph.py:185 calls context_size(); result drives _prompt_tier |
| `graph.py` | `supervisor.md` | `_build_system_prompt()` reads COMPACT section when tier=compact | WIRED | graph.py:290 compact branch; supervisor.md:1 has COMPACT section |
| `graph.py` | `action_parser.py` | `_parse_action_json` shim passes step=state['step'] | WIRED | graph.py:1004-1041 unpack `(action_data, used_fallback)`; fallback increments counter |
| `graph.py` | `handoff.py` | `create_handoff().model_dump()` before appending to handoff_queue | WIRED | graph.py:1592, 1632 confirmed |
| `graph.py` | `mission_context_store.py` | executor result handler calls upsert_cursor after read_file_chunk has_more=True | WIRED | graph.py:1899 `_store.upsert_cursor(...)` |
| `context_manager.py` | `mission_context_store.py` | `compact()` calls `self._store.get_active_cursors(run_id)` | WIRED | context_manager.py:598 confirmed |
| `graph.py` | `state_schema.py` | planner node increments `state['structural_health']['json_parse_fallback']` | WIRED | graph.py:1004-1005, 1040-1041 |
| `graph.py` | `mission_auditor.py` | `_finalize()` attaches structural_health into audit_report after audit_run() | WIRED | graph.py:2221-2224 |

---

### Requirements Coverage

No external requirement IDs — this is a structural hardening phase. All behaviors are covered by the 16 must-have truths above.

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| None found | — | — | — |

No NotImplementedError stubs remain in any of the 4 test files. No placeholder implementations detected. No TODO/FIXME/HACK markers found in modified source files that would block the goal.

---

### Human Verification Required

**1. Compact prompt on real phi4 / llama-cpp run**

**Test:** Set `P1_PROVIDER=ollama` with a small model that reports 8192 context (or set `LLAMA_CPP_N_CTX=8192` and `P1_PROVIDER=llama-cpp`), then run `make run`. Observe that the system prompt is the compact form.
**Expected:** No context overflow; run completes; prompt contains `## COMPACT` directive text.
**Why human:** Requires a live small-context model; cannot be verified programmatically without a real inference server.

**2. Fallback parser WARNING visible in logs**

**Test:** Configure a provider that returns XML-ish or prose-wrapped JSON; run an agent mission; grep the run log for `PARSER FALLBACK`.
**Expected:** WARNING lines appear with step number and truncated model output; `audit_report["structural_health"]["json_parse_fallback"] > 0` in the printed audit panel.
**Why human:** Requires a live provider that produces malformed JSON; ScriptedProvider always produces clean JSON.

**3. Cursor hint re-injection after context eviction**

**Test:** Run a multi-mission scenario that triggers ContextManager.compact() while a read_file_chunk loop is in progress. Check messages list post-compact.
**Expected:** An `[Orchestrator] Chunked read in progress` message appears in the messages list at the next planner step.
**Why human:** Requires an actual long-file scenario to trigger both the chunked read and the sliding window cap simultaneously.

---

### Gaps Summary

No gaps. All automated checks passed and all 1344 tests (unit + integration) pass cleanly.

The one minor discrepancy noted: Plan 04's `must_haves.artifacts` listed `mission_auditor.py` with `contains: "structural_health"`, but the correct implementation — as stated in the plan's own task description — attaches `structural_health` to the audit dict in `graph.py _finalize()` after `audit.to_dict()` returns, not inside the auditor itself. This is the right design (auditor stays pure) and the end-to-end test in `test_structural_health.py` proves the full `run() -> audit_report["structural_health"]` path works.

---

_Verified: 2026-03-09T15:14:40Z_
_Verifier: Claude (gsd-verifier)_
