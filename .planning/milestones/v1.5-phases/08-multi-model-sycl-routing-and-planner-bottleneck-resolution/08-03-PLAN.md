---
phase: 08-multi-model-sycl-routing-and-planner-bottleneck-resolution
plan: "03"
type: execute
wave: 3
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - src/agentic_workflows/orchestration/langgraph/orchestrator.py
  - src/agentic_workflows/orchestration/langgraph/planner_node.py
  - src/agentic_workflows/orchestration/langgraph/planner_helpers.py
  - src/agentic_workflows/orchestration/langgraph/executor_node.py
  - src/agentic_workflows/orchestration/langgraph/lifecycle_nodes.py
  - src/agentic_workflows/orchestration/langgraph/graph.py
autonomous: true
requirements:
  - SYCL-02

must_haves:
  truths:
    - "graph.py is a pure re-export shim — no logic, only imports forwarded from orchestrator.py"
    - "orchestrator.py contains LangGraphOrchestrator class with __init__, compile_graph, run()"
    - "planner_node.py + planner_helpers.py contain _plan_next_action() and all planner helper functions"
    - "executor_node.py contains _route_to_specialist(), _execute_action(), and execution helpers"
    - "lifecycle_nodes.py contains _finalize(), _enforce_memo_policy(), _clarify_node()"
    - "No single new file exceeds 600 lines"
    - "All 823+ existing tests pass unchanged after decomposition"
    - "All private symbols (LkrLangGraphOrchestrator, MemoizationPolicyViolation, _derive_annotated_list_fields, _ANNOTATED_LIST_FIELDS, _active_callbacks_var, _HANDOFF_QUEUE_CAP, _HANDOFF_RESULTS_CAP, _ROLE_TOKEN_BUDGETS, _select_prompt_tier, _estimate_prompt_tokens, _read_directive_section, _PIPELINE_TRACE_CAP) importable from graph.py via shim"
  artifacts:
    - path: "src/agentic_workflows/orchestration/langgraph/orchestrator.py"
      provides: "LangGraphOrchestrator class — the main spine of the orchestration platform"
      contains: "class LangGraphOrchestrator"
    - path: "src/agentic_workflows/orchestration/langgraph/planner_node.py"
      provides: "_plan_next_action() method logic extracted to standalone module"
      contains: "_plan_next_action"
    - path: "src/agentic_workflows/orchestration/langgraph/planner_helpers.py"
      provides: "Prompt-building and response-parsing helpers split from planner_node.py to meet 600-line limit"
    - path: "src/agentic_workflows/orchestration/langgraph/executor_node.py"
      provides: "_route_to_specialist() + _execute_action() extracted"
      contains: "_execute_action"
    - path: "src/agentic_workflows/orchestration/langgraph/lifecycle_nodes.py"
      provides: "_finalize(), _enforce_memo_policy(), _clarify_node() extracted"
      contains: "_finalize"
    - path: "src/agentic_workflows/orchestration/langgraph/graph.py"
      provides: "Pure re-export shim — backward compat for all importers"
      contains: "from .orchestrator import"
  key_links:
    - from: "graph.py"
      to: "orchestrator.py"
      via: "from .orchestrator import LangGraphOrchestrator, ..."
      pattern: "from .orchestrator import"
    - from: "orchestrator.py"
      to: "planner_node.py"
      via: "helper functions called from _plan_next_action method on class"
      pattern: "planner_node"
---

<objective>
Decompose the 3280-line graph.py monolith into focused sub-modules while keeping graph.py as a pure re-export shim for backward compatibility.

Purpose: Make the orchestration codebase maintainable — no single file exceeds 600 lines, each file has a single concern. All 823+ existing tests continue to pass with zero import churn.
Output: orchestrator.py (spine), planner_node.py + planner_helpers.py, executor_node.py, lifecycle_nodes.py, graph.py (shim).

Note: This plan runs AFTER Plan 02 (wave 3) because Plan 02 modifies graph.py to add _planner_provider/_executor_provider. orchestrator.py must be created from the Plan-02-modified graph.py so those changes are included. Running in parallel would cause orchestrator.py to miss Plan 02's SYCL-01 port-routing additions.
</objective>

<execution_context>
@/home/nir/.claude/get-shit-done/workflows/execute-plan.md
@/home/nir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-multi-model-sycl-routing-and-planner-bottleneck-resolution/08-CONTEXT.md
@.planning/phases/08-multi-model-sycl-routing-and-planner-bottleneck-resolution/08-RESEARCH.md
@.planning/phases/08-multi-model-sycl-routing-and-planner-bottleneck-resolution/08-01-SUMMARY.md
@.planning/phases/08-multi-model-sycl-routing-and-planner-bottleneck-resolution/08-02-SUMMARY.md

<interfaces>
<!-- Critical constraints for the decomposition. -->

COMPLETE LIST of private symbols that must be re-exported from graph.py shim
(derived from grep of all test files — every single one must work):
- LangGraphOrchestrator
- MemoizationPolicyViolation
- _derive_annotated_list_fields
- _ANNOTATED_LIST_FIELDS
- _active_callbacks_var
- _HANDOFF_QUEUE_CAP
- _HANDOFF_RESULTS_CAP
- _ROLE_TOKEN_BUDGETS
- _select_prompt_tier
- _estimate_prompt_tokens
- _read_directive_section
- _PIPELINE_TRACE_CAP
- (module-level import as `graph_module` in test_observability.py — the shim must be importable as a module)

Note on _PIPELINE_TRACE_CAP: imported in tests/unit/test_run_helpers.py from graph.py. It is a module-level integer constant in graph.py controlling pipeline trace output cap. Must be included in orchestrator.py exports AND in the graph.py shim re-export list. Run the grep below before writing the shim to catch any additional symbols not listed here.

ANTI-PATTERN (per RESEARCH.md Pitfall 6 — circular imports):
- Node files must NOT import from graph.py (circular)
- Node files import from state_schema, context_manager, provider, etc. directly
- The class methods stay ON LangGraphOrchestrator in orchestrator.py
- "What moves to node files" = pure helper FUNCTIONS that do not reference self
- This eliminates circular imports entirely

DECOMPOSITION STRATEGY (per CONTEXT.md locked decision):
- Each major LangGraph node becomes its own file
- Helpers co-located with their node
- LangGraphOrchestrator moves to orchestrator.py (thin spine)
- graph.py = re-export shim only

PLANNER SPLIT REQUIRED (per RESEARCH.md Pitfall 2):
- _plan_next_action() is 1078 lines — cannot fit in planner_node.py under 600-line limit
- Move prompt-building helpers (~300 lines) and response-parsing helpers (~200 lines) to planner_helpers.py
- planner_node.py contains _plan_next_action() calling into planner_helpers.py functions

FILE SIZE TARGETS:
| File | Target |
|------|--------|
| orchestrator.py | ~400 lines |
| planner_node.py | ≤600 lines |
| planner_helpers.py | ≤600 lines |
| executor_node.py | ≤600 lines (split to executor_helpers.py if needed) |
| lifecycle_nodes.py | ~200 lines |
| graph.py (shim) | ~30 lines |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit graph.py, plan the split, create orchestrator.py + node files</name>
  <files>
    src/agentic_workflows/orchestration/langgraph/orchestrator.py,
    src/agentic_workflows/orchestration/langgraph/planner_node.py,
    src/agentic_workflows/orchestration/langgraph/planner_helpers.py,
    src/agentic_workflows/orchestration/langgraph/executor_node.py,
    src/agentic_workflows/orchestration/langgraph/lifecycle_nodes.py
  </files>
  <action>
    STEP 1: Read graph.py completely to understand all method line ranges.

    STEP 2: Plan the split using the method list from graph.py grep:
    - orchestrator.py: LangGraphOrchestrator class with __init__ (line 216), _compile_graph (611), run() (implied at bottom), module-level constants and helpers (_ANNOTATED_LIST_FIELDS, _active_callbacks_var, _HANDOFF_QUEUE_CAP, _HANDOFF_RESULTS_CAP, _ROLE_TOKEN_BUDGETS, _select_prompt_tier, _estimate_prompt_tokens, _read_directive_section, MemoizationPolicyViolation, _derive_annotated_list_fields, _sequential_node, _PIPELINE_TRACE_CAP)
    - planner_helpers.py: pure helper functions for prompt building (_build_codebase_context, _build_action_json_schema, _build_system_prompt, _build_lc_tools) and response parsing (_validate_action, _parse_action_json, _extract_first_json_object, _extract_all_json_objects, _parse_all_actions_json, _validate_action_from_dict)
    - planner_node.py: _plan_next_action() method calling planner_helpers functions; planner-specific private methods (_log_queue_mission_spacing, _planner_action_preview, _log_parser_state, _log_planner_output, _generate_with_hard_timeout, _rerun_target_mission_ids, _rerun_targets_completed, _enforce_rerun_completion_guard, _purge_queued_finish_actions, _reject_finish_and_recover, _reset_finish_rejection_tracking, _env_float, _env_bool)
    - executor_node.py: _route_to_specialist(), _execute_action(), _select_specialist_for_action(), _is_tool_allowed_for_specialist(), _dedup_then_tool_node(), _is_unrecoverable_plan_error(), _mission_preview_from_state(), _resolve_mission_id_for_action(), _deterministic_fallback_action(), _extract_quoted_text(), _extract_numbers_from_text(), _extract_fibonacci_count(), _fibonacci_csv(), _extract_missions(), _infer_requirements_from_text(), _build_mission_contracts_from_plan()
    - lifecycle_nodes.py: _finalize(), _write_shared_plan(), _enforce_memo_policy(), _clarify_node(), _diagnose_incomplete_missions(), _emit_trace(), _route_after_plan(), _invalidate_known_poisoned_cache_entries()

    STEP 3: The key constraint — methods stay ON the LangGraphOrchestrator class (they reference self). The split is about which FILE the method lives in, not making them standalone functions. Since Python doesn't support class method definition across files natively, the approach is:

    Option A (RECOMMENDED): Keep ALL methods in orchestrator.py. Move only genuine module-level pure functions to helper files. The node-specific pure helpers (e.g., _build_codebase_context which is @staticmethod, _select_prompt_tier which is module-level) move to their helper files, imported back into orchestrator.py.

    Option B: Use a mixin pattern — define method groups in mixin classes in separate files, have LangGraphOrchestrator inherit from them.

    CHOOSE Option A — it is simpler, avoids MRO complexity, and all existing tests still pass. The result:
    - orchestrator.py: LangGraphOrchestrator class + all its methods + module-level constants
    - planner_helpers.py: module-level functions _build_codebase_context (staticmethod body), _select_prompt_tier, _estimate_prompt_tokens, _read_directive_section, _build_action_json_schema helpers
    - The class methods in orchestrator.py call these functions, keeping orchestrator.py as the single source of truth for the class

    IMPORTANT LINE COUNT CHECK: After initial split, run `wc -l` on each file. If orchestrator.py still exceeds 600 lines, continue moving pure module-level functions to helper files. The goal is: orchestrator.py ≤ 600, all other files ≤ 600.

    If orchestrator.py still exceeds 600 lines after extracting pure functions, use the mixin approach:
    - Create `_planner_mixin.py` with a `PlannerMixin` class containing all `_plan_*` methods
    - Create `_executor_mixin.py` with an `ExecutorMixin` class containing all `_execute_*` and `_route_*` methods
    - orchestrator.py: `class LangGraphOrchestrator(PlannerMixin, ExecutorMixin): ...`
    - This is a clean pattern that does NOT cause circular imports

    STEP 4: Create orchestrator.py first (copy from graph.py, keep it complete and working).
    STEP 5: Create helper files with extracted pure functions.
    STEP 6: Update imports in orchestrator.py to use the extracted functions.
    STEP 7: Verify each file is ≤ 600 lines. Split further if needed.
    STEP 8: Do NOT modify graph.py yet — that is Task 2.

    Run after each file: `pytest tests/unit/ -q -x` to catch import breaks early.
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && wc -l src/agentic_workflows/orchestration/langgraph/orchestrator.py src/agentic_workflows/orchestration/langgraph/planner_node.py src/agentic_workflows/orchestration/langgraph/planner_helpers.py src/agentic_workflows/orchestration/langgraph/executor_node.py src/agentic_workflows/orchestration/langgraph/lifecycle_nodes.py 2>/dev/null && pytest tests/unit/ -q -x 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 new files exist; no single file exceeds 600 lines; pytest tests/unit/ -q -x passes (tests still import from graph.py which is unchanged)</done>
</task>

<task type="auto">
  <name>Task 2: Convert graph.py to re-export shim and run full regression</name>
  <files>src/agentic_workflows/orchestration/langgraph/graph.py</files>
  <action>
    Replace graph.py content with a pure re-export shim. It must re-export EVERY symbol that any test imports from it.

    Before writing the shim, verify the complete symbol list by running:
    ```bash
    grep -rn "from agentic_workflows.orchestration.langgraph.graph import" tests/ | grep -oP "(?<=import ).*" | tr ',' '\n' | sed 's/[() ]//g' | sort -u
    ```

    The shim must re-export at minimum:
    - LangGraphOrchestrator
    - MemoizationPolicyViolation
    - _derive_annotated_list_fields
    - _ANNOTATED_LIST_FIELDS
    - _active_callbacks_var
    - _HANDOFF_QUEUE_CAP
    - _HANDOFF_RESULTS_CAP
    - _ROLE_TOKEN_BUDGETS
    - _select_prompt_tier
    - _estimate_prompt_tokens
    - _read_directive_section
    - _PIPELINE_TRACE_CAP

    The shim format (per RESEARCH.md code example):
    ```python
    # graph.py — backward-compat re-export shim for Phase 8 decomposition
    # All logic lives in orchestrator.py and helper modules.
    # DO NOT add logic here — this file is a shim only.
    from .orchestrator import (
        LangGraphOrchestrator,
        MemoizationPolicyViolation,
        _ANNOTATED_LIST_FIELDS,
        _HANDOFF_QUEUE_CAP,
        _HANDOFF_RESULTS_CAP,
        _ROLE_TOKEN_BUDGETS,
        _PIPELINE_TRACE_CAP,
        _active_callbacks_var,
        _derive_annotated_list_fields,
        _select_prompt_tier,
        _estimate_prompt_tokens,
        _read_directive_section,
        # add any other symbols revealed by the grep above
    )

    __all__ = [
        "LangGraphOrchestrator",
        "MemoizationPolicyViolation",
        "_ANNOTATED_LIST_FIELDS",
        "_HANDOFF_QUEUE_CAP",
        "_HANDOFF_RESULTS_CAP",
        "_ROLE_TOKEN_BUDGETS",
        "_PIPELINE_TRACE_CAP",
        "_active_callbacks_var",
        "_derive_annotated_list_fields",
        "_select_prompt_tier",
        "_estimate_prompt_tokens",
        "_read_directive_section",
    ]
    ```

    After writing the shim, run the full test suite. Fix any ImportError by tracing which symbol is missing from the shim or from orchestrator.py's exports.

    CRITICAL: The test_observability.py imports the graph module itself:
    `from agentic_workflows.orchestration.langgraph import graph as graph_module`
    This works as long as graph.py is a valid Python module (which the shim is).

    Run ruff check on the shim file to verify it is clean.
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && pytest tests/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>pytest tests/ -q passes all 823+ tests; no ImportError from graph.py; wc -l graph.py shows ≤50 lines (shim only); ruff check clean on all modified files</done>
</task>

</tasks>

<verification>
- wc -l on all new files — none exceeds 600 lines
- pytest tests/ -q — all 823+ tests pass (SYCL-02 success criterion)
- python -c "from agentic_workflows.orchestration.langgraph.graph import LangGraphOrchestrator, _derive_annotated_list_fields, _ANNOTATED_LIST_FIELDS, _active_callbacks_var, _HANDOFF_QUEUE_CAP, _HANDOFF_RESULTS_CAP, _ROLE_TOKEN_BUDGETS, _select_prompt_tier, _estimate_prompt_tokens, _read_directive_section, MemoizationPolicyViolation, _PIPELINE_TRACE_CAP; print('all symbols importable')"
- ruff check src/agentic_workflows/orchestration/langgraph/graph.py orchestrator.py planner_node.py planner_helpers.py executor_node.py lifecycle_nodes.py
</verification>

<success_criteria>
- graph.py is a shim (≤50 lines) re-exporting all required symbols including _PIPELINE_TRACE_CAP
- orchestrator.py contains LangGraphOrchestrator and is ≤600 lines
- All other new files are ≤600 lines each
- All 823+ existing tests pass unchanged (SYCL-02 proven)
- ruff clean on all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-model-sycl-routing-and-planner-bottleneck-resolution/08-03-SUMMARY.md`
</output>
