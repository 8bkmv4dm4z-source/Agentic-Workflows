---
phase: 02-langgraph-upgrade-and-single-agent-hardening
plan: 05
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: []

must_haves:
  truths:
    - "GitHub Actions CI workflow file exists at .github/workflows/ci.yml"
    - "The workflow runs ruff check, mypy, and pytest on every push and pull_request to main"
    - "No live LLM API keys are required — ScriptedProvider handles all LLM interaction"
    - "Python 3.12 is used to match the dev environment"
    - "The workflow is separate from the existing claude.yml (not modified)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Full-suite CI gate (lint + typecheck + test)"
      contains: "pytest tests/ -q"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pip install -e .[dev]"
      via: "Install step"
      pattern: "pip install"
    - from: "CI test step"
      to: "ScriptedProvider"
      via: "pytest tests/ -q (no API keys in env)"
      pattern: "pytest tests"
---

<objective>
Create `.github/workflows/ci.yml` — a GitHub Actions workflow that runs `ruff check`, `mypy`, and `pytest` on every push and pull request. Zero live LLM calls; `ScriptedProvider` handles all test LLM interaction.

Purpose: Automated quality gate prevents regressions from landing on any branch. Required by Phase 2 success criterion #5. Independent of all code changes in this phase — can be created in parallel with Plans 02, 03, 04.

Output: `.github/workflows/ci.yml` configured and ready to trigger on push.
</objective>

<execution_context>
@/home/nir/.claude/get-shit-done/workflows/execute-plan.md
@/home/nir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-langgraph-upgrade-and-single-agent-hardening/02-CONTEXT.md
@.planning/phases/02-langgraph-upgrade-and-single-agent-hardening/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create GitHub Actions CI workflow</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <action>
First, verify the .github/workflows/ directory exists:
```
ls /home/nir/dev/agent_phase0/.github/workflows/
```
(claude.yml should be there — do NOT modify it.)

Create `.github/workflows/ci.yml` with the following content:

```yaml
name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

jobs:
  quality:
    name: Lint, Typecheck, Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Lint (ruff)
        run: ruff check src/ tests/

      - name: Typecheck (mypy)
        run: mypy src/

      - name: Test (pytest)
        run: pytest tests/ -q
        env:
          # ScriptedProvider is the test double for all LLM calls.
          # No live provider API keys are needed or used in the test suite.
          P1_PROVIDER: scripted
```

Key decisions in this file:
- `branches: ["**"]` on push — catches all branches including feature branches
- `pull_request: branches: [main]` — PR gate for the main branch
- No pip cache in v1 (per CONTEXT.md decision — keep it simple; caching added in Phase 7)
- `P1_PROVIDER: scripted` in env ensures the test suite uses ScriptedProvider and cannot accidentally call live APIs even if keys were present
- No API key secrets are defined — confirms no live LLM calls possible
- Single job named `quality` with three sequential steps: lint → typecheck → test

After writing the file, validate the YAML syntax locally if `yamllint` is available:
```
yamllint .github/workflows/ci.yml 2>/dev/null || echo "yamllint not available — validate manually"
```

Also confirm the existing claude.yml is unchanged:
```
cat /home/nir/dev/agent_phase0/.github/workflows/claude.yml | head -5
```
  </action>
  <verify>
    <automated>cat /home/nir/dev/agent_phase0/.github/workflows/ci.yml | grep -c "ruff\|mypy\|pytest"</automated>
  </verify>
  <done>.github/workflows/ci.yml exists and contains ruff, mypy, and pytest steps. claude.yml is unchanged.</done>
</task>

</tasks>

<verification>
1. `ls .github/workflows/` — shows both `claude.yml` (unchanged) and `ci.yml` (new)
2. `grep "pytest tests/ -q" .github/workflows/ci.yml` — matches
3. `grep "LANGFUSE\|OPENAI_API_KEY\|GROQ_API_KEY\|ANTHROPIC_API_KEY" .github/workflows/ci.yml` — NO match (no live API keys in CI)
4. `grep "python-version" .github/workflows/ci.yml` — shows 3.12
5. `grep "P1_PROVIDER: scripted" .github/workflows/ci.yml` — matches
</verification>

<success_criteria>
`.github/workflows/ci.yml` is created with ruff check, mypy, and pytest steps. Push trigger covers all branches. No live API keys. Uses Python 3.12 and ScriptedProvider. The existing claude.yml is untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/02-langgraph-upgrade-and-single-agent-hardening/02-05-SUMMARY.md` with:
- Path to the ci.yml file
- Trigger conditions (push all branches + PR to main)
- The three quality gate steps and their commands
- Confirmation that no API secrets are required
</output>
