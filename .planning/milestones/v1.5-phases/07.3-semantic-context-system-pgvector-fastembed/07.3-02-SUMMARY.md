---
phase: 07.3-semantic-context-system-pgvector-fastembed
plan: "02"
subsystem: context
tags: [embedding, fastembed, protocol, mock, testing, python]

requires:
  - phase: 07.3-00
    provides: migration stubs and test RED stubs for embedding_provider, artifact_store, mission_context_store

provides:
  - EmbeddingProvider Protocol (runtime_checkable structural typing)
  - MockEmbeddingProvider (SHA-256 seeded, 384-dim, deterministic, CI-safe)
  - FastEmbedProvider (lazy fastembed import, helpful ImportError message)
  - get_embedding_provider() factory reading EMBEDDING_PROVIDER env var
  - context package importable as agentic_workflows.context
  - fastembed>=0.3 optional dep group [context] in pyproject.toml

affects:
  - 07.3-03 (MissionContextStore uses EmbeddingProvider for semantic search)
  - 07.3-04 (ArtifactStore may use EmbeddingProvider)
  - orchestration/langgraph/context_manager.py (optional embedding_provider param)
  - storage/mission_context_store.py (EmbeddingProvider injection)

tech-stack:
  added: [fastembed>=0.3 (optional dep, not installed by default)]
  patterns:
    - Structural Protocol typing for provider abstraction (runtime_checkable)
    - SHA-256 seeded RNG for deterministic unit-norm embeddings in tests
    - Lazy import pattern in __init__ for optional heavy dependencies
    - Factory function reading env var for provider selection

key-files:
  created:
    - src/agentic_workflows/context/__init__.py
    - src/agentic_workflows/context/embedding_provider.py
  modified:
    - pyproject.toml

key-decisions:
  - "SHA-256 seed into random.Random for MockEmbeddingProvider — same text always yields identical unit-norm 384-dim vector, no collision risk for test isolation"
  - "fastembed import deferred to FastEmbedProvider.__init__ body — context package importable in CI without the optional dep installed"
  - "get_embedding_provider() lowercases and strips EMBEDDING_PROVIDER value — tolerates case/whitespace variance in env"
  - "fastembed placed in [context] optional dep group (not [dev]) — production opt-in without polluting base install"

patterns-established:
  - "Lazy optional import: try/except ImportError in __init__, raise with pip install hint"
  - "Env-driven factory: os.environ.get with .lower().strip() + explicit ValueError for unknown values"
  - "Deterministic mock via hashlib seed: SHA-256 digest as int seed for random.Random"

requirements-completed: [SCS-01, SCS-02]

duration: 2min
completed: 2026-03-08
---

# Phase 07.3 Plan 02: Embedding Provider Summary

**SHA-256-seeded MockEmbeddingProvider and lazy-import FastEmbedProvider behind EmbeddingProvider Protocol, with [context] optional dep group in pyproject.toml**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-03-08T16:34:39Z
- **Completed:** 2026-03-08T16:36:09Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Implemented EmbeddingProvider structural Protocol (runtime_checkable) — any duck-typed class satisfying the interface passes isinstance checks
- MockEmbeddingProvider uses SHA-256 hash of input text as RNG seed: deterministic 384-dim unit-norm vectors, CI-safe, no model download
- FastEmbedProvider defers fastembed import to __init__ body — helpful ImportError raised when package not installed, all other imports unaffected
- get_embedding_provider() factory reads EMBEDDING_PROVIDER env var with graceful lowercasing and ValueError for unknown values
- All 10 test_embedding_provider.py tests pass GREEN (plan projected 9; actual file has 10)
- fastembed>=0.3 added as [context] optional dep group — production opt-in without polluting base or dev installs

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement context package and embedding_provider.py** - `3918b7f` (feat)
2. **Task 2: Add fastembed optional dep to pyproject.toml** - `b0b06df` (chore)

## Files Created/Modified
- `src/agentic_workflows/context/__init__.py` - Package init with module docstring
- `src/agentic_workflows/context/embedding_provider.py` - EmbeddingProvider Protocol, MockEmbeddingProvider, FastEmbedProvider, get_embedding_provider()
- `pyproject.toml` - Added [context] optional dep group with fastembed>=0.3

## Decisions Made
- SHA-256 seed into random.Random ensures identical output for identical input across Python runs (unlike default random seeding)
- fastembed import deferred to __init__ body (not module-level) so the context package loads cleanly in CI
- get_embedding_provider() applies .lower().strip() before matching — tolerates "Mock", "MOCK", " mock " from env
- [context] dep group chosen over [dev] because it is a production opt-in, not a test-only tool

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Pre-existing collection errors in test_artifact_store.py and test_mission_context_store.py (RED stubs from plan 07.3-01 for future plans 07.3-03/04) — not caused by this plan, documented and skipped per deviation scope rules. 1166 tests pass when these are excluded.

## Next Phase Readiness
- EmbeddingProvider Protocol is ready for injection into MissionContextStore (plan 07.3-03)
- MockEmbeddingProvider is the default provider for all existing tests — no test infrastructure changes needed
- FastEmbedProvider is gated behind optional dep and lazy import — safe to merge without requiring fastembed installation

---
*Phase: 07.3-semantic-context-system-pgvector-fastembed*
*Completed: 2026-03-08*
