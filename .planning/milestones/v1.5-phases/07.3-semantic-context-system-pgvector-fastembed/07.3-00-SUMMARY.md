---
phase: 07.3-semantic-context-system-pgvector-fastembed
plan: "00"
subsystem: test-infrastructure
tags: [tdd, test-stubs, embedding-provider, mission-context-store, artifact-store, wave-0]
dependency_graph:
  requires: []
  provides: [test-stubs-embedding-provider, test-stubs-mission-context-store, test-stubs-artifact-store, clean-pg-phase-7.3]
  affects: [tests/conftest.py, tests/unit/]
tech_stack:
  added: []
  patterns: [TDD-RED, Nyquist-compliance, wave-0-infrastructure]
key_files:
  created:
    - tests/unit/test_embedding_provider.py
    - tests/unit/test_mission_context_store.py
    - tests/unit/test_artifact_store.py
  modified:
    - tests/conftest.py
decisions:
  - "All 3 new test stubs fail at collection time (ImportError) — correct RED state before Wave 1B/2A implementations"
  - "clean_pg fixture extended with per-table try/except for mission_contexts/mission_artifacts — graceful before migrations 003/004"
metrics:
  duration: "~2 min"
  completed: "2026-03-08"
  tasks: 2
  files: 4
---

# Phase 7.3 Plan 00: TDD Wave 0 — Failing test stubs for EmbeddingProvider, MissionContextStore, and ArtifactStore

**One-liner:** Created 21 failing RED test stubs across 3 new test files covering SCS-01 through SCS-07, with clean_pg extended for Phase 7.3 tables.

## What Was Built

Wave 0 (Nyquist compliance): Pre-existing failing tests for all Phase 7.3 production modules before any implementation exists.

### Files Created

**tests/unit/test_embedding_provider.py** (9 tests, all RED)
- `TestMockEmbeddingProvider`: 5 tests — dimensions, determinism, different inputs, unit norm, batch embed
- `TestFastEmbedProvider`: 1 test — import guard raises ImportError with helpful message when fastembed not installed
- `TestGetEmbeddingProvider`: 4 tests — env routing for default, "mock", "fastembed", and unknown provider

**tests/unit/test_mission_context_store.py** (10 tests, all RED)
- `TestEncodeToolPattern`: 4 tests — known tools (bit values), unknown ignored, empty list, bits unique
- `TestSha256`: 3 tests — determinism, different inputs, hex string format (64 chars)
- `TestReciprocalRankFusion`: 4 tests — known input (RRF scoring), single list, empty, k parameter
- `TestMissionContextStoreFallback`: 2 tests — query_cascade and upsert no-op when pool=None

**tests/unit/test_artifact_store.py** (2 tests, all RED)
- `TestArtifactStoreFallback`: 2 tests — upsert and search no-op when pool=None

### Files Modified

**tests/conftest.py** — `clean_pg` fixture extended with graceful truncation of `mission_contexts` and `mission_artifacts` tables via per-table try/except blocks (safe before migrations 003/004 have run).

## Verification

- All 3 new test files fail at collection time with `ModuleNotFoundError` (expected RED state)
- 1156 existing unit tests continue to pass (no regressions)
- `grep -n "mission_contexts\|mission_artifacts" tests/conftest.py` shows line 98 with new table names

## Deviations from Plan

None — plan executed exactly as written.

## Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | EmbeddingProvider stubs + conftest extension | 431996b | tests/unit/test_embedding_provider.py, tests/conftest.py |
| 2 | MissionContextStore + ArtifactStore stubs | 36e0c1c | tests/unit/test_mission_context_store.py, tests/unit/test_artifact_store.py |

## Self-Check: PASSED

- tests/unit/test_embedding_provider.py: EXISTS
- tests/unit/test_mission_context_store.py: EXISTS
- tests/unit/test_artifact_store.py: EXISTS
- tests/conftest.py: mission_contexts/mission_artifacts present at line 98
- Commit 431996b: EXISTS
- Commit 36e0c1c: EXISTS
- 1156 existing tests: PASS
