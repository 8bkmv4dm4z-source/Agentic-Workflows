---
phase: 07.6-llm-output-structure-stabilization
plan: "04"
subsystem: orchestration
tags: [structural-health, observability, metrics, action-parser, audit-report, langgraph]

# Dependency graph
requires:
  - phase: 07.6-01
    provides: parse_action_json step parameter and WARNING fallback logging
  - phase: 07.6-02
    provides: parse_action_json fallback path infrastructure
  - phase: 07.6-03
    provides: Wave 0 test stubs for structural_health in test_structural_health.py
provides:
  - structural_health dict in RunState (json_parse_fallback + schema_mismatch counters)
  - parse_action_json returns (dict, bool) tuple; fallback bool enables counter tracking
  - parse_all_actions_json returns (list[dict], bool) tuple for planner-level fallback tracking
  - audit_report["structural_health"] present after every run via _finalize()
  - .env.example LLAMA_CPP_GRAMMAR comment clarified (grammar enabled by default)
affects: [07.6-05, observability, audit, any code calling parse_action_json or validate_action]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "(dict, bool) tuple return from parse_action_json/validate_action/validate_action_from_dict — propagates fallback signal without global state"
    - "Structural health metrics as side-channel in RunState — parallel to retry_counts, separate from audit findings"
    - "_finalize() post-processes audit_report dict after to_dict() — consistent with existing pattern for run_id injection"

key-files:
  created: []
  modified:
    - src/agentic_workflows/orchestration/langgraph/state_schema.py
    - src/agentic_workflows/orchestration/langgraph/action_parser.py
    - src/agentic_workflows/orchestration/langgraph/graph.py
    - .env.example
    - tests/unit/test_structural_health.py
    - tests/unit/test_action_parser.py
    - tests/unit/test_action_queue.py

key-decisions:
  - "parse_all_actions_json also returns (list, bool) tuple — planner uses this function not parse_action_json directly; fallback bool=True when json.loads fails on full output and extract_all_json_objects fallback is used"
  - "schema_mismatch increment placed in except Exception block in _plan_next_action — catches ValueError from validate_action when Pydantic rejects the schema"
  - "structural_health initialized in both new_run_state() and ensure_state_defaults() — ensures presence whether state is fresh or restored from checkpoint"
  - "test_action_queue.py ParseAllActionsJsonTests updated to unpack tuple — deviation Rule 3 (blocking), fixed inline"

patterns-established:
  - "Tuple return pattern for parse_action_json/validate_action: always unpack with _, _ = before asserting on dict fields"
  - "Structural health counters use .get(key, 0) + 1 pattern — defensive against partial state"

requirements-completed: []

# Metrics
duration: 6min
completed: 2026-03-09
---

# Phase 07.6 Plan 04: Structural Health Metrics Summary

**Structural health metrics (json_parse_fallback + schema_mismatch counters) wired into RunState and exposed in audit_report via _finalize(); parse_action_json family refactored to return (dict, bool) tuple for fallback signal propagation**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-09T17:03:39Z
- **Completed:** 2026-03-09T17:09:30Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- Added `structural_health: dict[str, Any]` to RunState TypedDict with `json_parse_fallback` and `schema_mismatch` counters
- Refactored `parse_action_json`, `parse_all_actions_json`, `validate_action`, and `validate_action_from_dict` to return `(data, used_fallback)` tuples, enabling callers to track parser stress
- Wired counter increments in `_plan_next_action`: `json_parse_fallback` increments when `parse_all_actions_json` fallback fires; `schema_mismatch` increments on ValidationError
- Attached `structural_health` to `audit_report` in `_finalize()` — present after every run, zero-value baseline for clean runs
- Replaced three NotImplementedError stubs in `test_structural_health.py` with full end-to-end assertions via `run()` → `RunResult.audit_report["structural_health"]`
- Clarified `LLAMA_CPP_GRAMMAR` comment in `.env.example` (grammar enabled by default)

## Task Commits

1. **Task 1: structural_health + parse_action_json tuple refactor + test_structural_health GREEN** - `24b1bca` (feat)
2. **Task 2: .env.example grammar comment** - `459853f` (chore)

## Files Created/Modified

- `src/agentic_workflows/orchestration/langgraph/state_schema.py` — Added `structural_health` field to RunState TypedDict; initialized in `new_run_state()` and `ensure_state_defaults()`
- `src/agentic_workflows/orchestration/langgraph/action_parser.py` — `parse_action_json`, `parse_all_actions_json`, `validate_action`, `validate_action_from_dict` all return `(data, bool)` tuples
- `src/agentic_workflows/orchestration/langgraph/graph.py` — Shims updated to return tuples; planner increments `json_parse_fallback` and `schema_mismatch`; `_finalize()` attaches `structural_health` to `audit_report`
- `.env.example` — `LLAMA_CPP_GRAMMAR` comment clarified: grammar enabled by default, set 0 to disable
- `tests/unit/test_structural_health.py` — NotImplementedError stubs replaced with real run-level assertions including `_RawStringProvider` for fallback triggering
- `tests/unit/test_action_parser.py` — All call sites updated to unpack tuple; two new flag-assertion tests added
- `tests/unit/test_action_queue.py` — `ParseAllActionsJsonTests` updated to unpack `_parse_all_actions_json` tuple

## Decisions Made

- `parse_all_actions_json` also refactored to return tuple (not only `parse_action_json`) because the planner node calls `_parse_all_actions_json`, not `_parse_action_json` — this is where the fallback actually fires in production
- `schema_mismatch` increment placed in the outer `except Exception` block in `_plan_next_action` — matches where ValidationError propagates as `ValueError("tool schema error: ...")`
- Used `.get(key, 0) + 1` pattern for counter increments — defensive against partial state after checkpoint restore

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed test_action_queue.py to unpack _parse_all_actions_json tuple**
- **Found during:** Task 1 (full regression run after implementation)
- **Issue:** `ParseAllActionsJsonTests` called `self.orch._parse_all_actions_json(text)` and directly indexed the return value as a list — TypeError after tuple return change
- **Fix:** Updated 4 test methods in `ParseAllActionsJsonTests` to unpack `result, _ = self.orch._parse_all_actions_json(text)`
- **Files modified:** `tests/unit/test_action_queue.py`
- **Verification:** `1344 passed` after fix (was 2 failures before)
- **Committed in:** `24b1bca` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 3 - blocking)
**Impact on plan:** `test_action_queue.py` was a direct consequence of the `_parse_all_actions_json` tuple return change. Required to unblock full test suite passage. No scope creep.

## Issues Encountered

None — implementation proceeded cleanly. The key design insight was that `parse_all_actions_json` (not `parse_action_json`) is the actual planner call site, so it also needed the tuple return for the counter to work end-to-end.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `audit_report["structural_health"]` now available for all Wave 0 structural health assertions
- All Wave 0 stubs in `test_structural_health.py` are green
- `parse_action_json` tuple return is a new API contract — downstream callers outside graph.py should unpack if they call it directly
- Ready for Wave 3 final plan (07.6-05) or any remaining phase plans

---
*Phase: 07.6-llm-output-structure-stabilization*
*Completed: 2026-03-09*
