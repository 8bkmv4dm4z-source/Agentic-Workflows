---
phase: 08.1-handle-the-concerns-of-the-planning-codebase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agentic_workflows/orchestration/langgraph/lifecycle_nodes.py
  - src/agentic_workflows/orchestration/langgraph/README.md
  - src/agentic_workflows/orchestration/langgraph/model_router.py
  - tests/unit/test_model_router_wiring.py
autonomous: true
requirements: [dead-code-01, dead-code-02]

must_haves:
  truths:
    - "_parse_action_json shim is gone from lifecycle_nodes.py with zero callers remaining"
    - "route_by_intent() is gone from model_router.py and its 4 test callers are removed"
    - "All 823+ tests continue to pass after removals"
    - "ruff check src/ is clean after removals"
  artifacts:
    - path: "src/agentic_workflows/orchestration/langgraph/lifecycle_nodes.py"
      provides: "LifecycleNodesMixin without _parse_action_json shim"
    - path: "src/agentic_workflows/orchestration/langgraph/model_router.py"
      provides: "ModelRouter without route_by_intent() deprecated shim"
    - path: "tests/unit/test_model_router_wiring.py"
      provides: "Model router tests without route_by_intent backward-compat section"
  key_links:
    - from: "lifecycle_nodes.py"
      to: "action_parser.parse_action_json"
      via: "direct import (existing callers already use action_parser directly)"
      pattern: "action_parser\\.parse_action_json"
    - from: "model_router.py"
      to: "route_by_signals"
      via: "all routing flows through route_by_signals() after removal"
      pattern: "route_by_signals"
---

<objective>
Remove two confirmed dead-code items from the codebase: the `_parse_action_json` backward-compat shim in `lifecycle_nodes.py` (zero callers in src/) and the `route_by_intent()` deprecated shim in `model_router.py` (4 test callers, requires test update first). Also remove the reference to `_parse_action_json` from the langgraph README.md. Perform a light TODO/FIXME scan to confirm no other obviously safe removals exist.

Purpose: Close the tech-debt gaps flagged in the v1.5 milestone audit. Reduce surface area and eliminate misleading dead API from the codebase.
Output: Cleaner lifecycle_nodes.py (no shim), cleaner model_router.py (no deprecated method), updated tests, updated README.
</objective>

<execution_context>
@/home/nir/.claude/get-shit-done/workflows/execute-plan.md
@/home/nir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key facts verified against live source. -->

From lifecycle_nodes.py line 263 (shim to remove):
```python
def _parse_action_json(self, model_output: str, state: RunState | None = None) -> tuple[dict[str, Any], bool]:
    # backward-compat shim delegating to action_parser.parse_action_json()
    ...
```
Zero callers in src/ confirmed via grep. README.md line 60 references it.

From model_router.py lines 104–120 (shim to remove):
```python
def route_by_intent(self, intent_classification: dict | None, ...) -> ChatProvider:
    warnings.warn("route_by_intent() is deprecated; use route_by_signals()", DeprecationWarning, ...)
    ...
```
4 test callers in tests/unit/test_model_router_wiring.py lines 178–238 (section titled "Deprecated route_by_intent backward-compat tests").

Light scan result: Zero TODO/FIXME comments in src/ — no additional removals needed.

route_by_signals in planner_node.py and planner_helpers.py serve DIFFERENT purposes — NOT redundant. Do NOT remove either call.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove _parse_action_json shim from lifecycle_nodes.py and README.md</name>
  <files>src/agentic_workflows/orchestration/langgraph/lifecycle_nodes.py, src/agentic_workflows/orchestration/langgraph/README.md</files>
  <action>
    1. Confirm zero src/ callers: run `grep -rn "_parse_action_json" src/ tests/` — expect only lifecycle_nodes.py:263 (definition) and README.md:60 (reference). If any unexpected callers exist, stop and document.
    2. Delete the `_parse_action_json` method from `lifecycle_nodes.py`. Remove the entire method body including its docstring and any blank lines that become redundant. Do not touch surrounding methods.
    3. In `src/agentic_workflows/orchestration/langgraph/README.md` line 60, update the planning bullet from:
       `- Planning: \`_plan_next_action()\`, \`_validate_action()\`, \`_parse_action_json()\``
       to:
       `- Planning: \`_plan_next_action()\`, \`_validate_action()\``
    4. Run `ruff check src/agentic_workflows/orchestration/langgraph/lifecycle_nodes.py` to confirm no lint errors.
    5. Run `pytest tests/ -q --tb=short` to confirm no regressions.
  </action>
  <verify>
    <automated>grep -rn "_parse_action_json" /home/nir/dev/agent_phase0/src/ && echo "FAIL: still present" || echo "PASS: fully removed"; cd /home/nir/dev/agent_phase0 && python3 -m pytest tests/ -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>grep finds no occurrences of _parse_action_json in src/; all tests pass; ruff clean</done>
</task>

<task type="auto">
  <name>Task 2: Remove route_by_intent test callers then delete the deprecated shim</name>
  <files>tests/unit/test_model_router_wiring.py, src/agentic_workflows/orchestration/langgraph/model_router.py</files>
  <action>
    1. Confirm caller locations: run `grep -rn "route_by_intent" src/ tests/` — expect model_router.py (definition) and test_model_router_wiring.py lines ~178–238 (4 test functions).
    2. In `tests/unit/test_model_router_wiring.py`, delete the entire section starting with the comment `# --- Deprecated route_by_intent backward-compat tests ---` through the end of the last test in that section (`test_route_by_intent_none_planning_returns_strong`). Also delete the module-level docstring bullet that mentions `route_by_intent()` if present (line 9 mentions it).
    3. In `src/agentic_workflows/orchestration/langgraph/model_router.py`, delete the `route_by_intent()` method (including its docstring, `warnings.warn(...)` call, and `import warnings` if it becomes unused after removal).
    4. Run `ruff check src/agentic_workflows/orchestration/langgraph/model_router.py` to confirm clean.
    5. Run `pytest tests/unit/test_model_router_wiring.py -q --tb=short` to confirm remaining tests pass.
    6. Run `pytest tests/ -q --tb=short` full suite regression check.
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && grep -rn "route_by_intent" src/ && echo "FAIL: still present in src" || echo "PASS: removed from src"; python3 -m pytest tests/unit/test_model_router_wiring.py tests/ -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>route_by_intent appears in neither src/ nor tests/; all tests pass; ruff clean on model_router.py</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -rn "_parse_action_json" src/` returns nothing
- `grep -rn "route_by_intent" src/ tests/` returns nothing
- `pytest tests/ -q` passes with 823+ tests green
- `ruff check src/` is clean
</verification>

<success_criteria>
Both deprecated shims are deleted from src/. Their test callers are removed. All existing tests continue to pass. ruff check is clean. The README.md no longer references the removed method.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-handle-the-concerns-of-the-planning-codebase/08.1-01-SUMMARY.md`
</output>
