---
phase: 07.1-context-manipulation-for-better-sub-agent-multi-task-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agentic_workflows/orchestration/langgraph/context_manager.py
  - src/agentic_workflows/orchestration/langgraph/state_schema.py
  - tests/unit/test_context_manager.py
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03, CTX-09]

must_haves:
  truths:
    - "MissionContext Pydantic model can be created, serialized, and validated"
    - "Mission completion produces a deterministic summary from structured data"
    - "Tool results yield extractable cross-mission artifacts"
    - "Artifacts from earlier missions are available to later missions via MissionContext"
  artifacts:
    - path: "src/agentic_workflows/orchestration/langgraph/context_manager.py"
      provides: "MissionContext, ArtifactRecord, SubMissionContext models + ContextManager skeleton + artifact extractors"
      exports: ["MissionContext", "ArtifactRecord", "SubMissionContext", "ContextManager"]
    - path: "src/agentic_workflows/orchestration/langgraph/state_schema.py"
      provides: "mission_contexts field on RunState + default in new_run_state/ensure_state_defaults"
      contains: "mission_contexts"
    - path: "tests/unit/test_context_manager.py"
      provides: "Unit tests for CTX-01, CTX-02, CTX-03, CTX-09"
      min_lines: 80
  key_links:
    - from: "src/agentic_workflows/orchestration/langgraph/context_manager.py"
      to: "state_schema.py"
      via: "MissionContext stored as dict[str, Any] in RunState.mission_contexts"
      pattern: "mission_contexts"
---

<objective>
Create the MissionContext Pydantic model, artifact extraction system, deterministic summary generation, and wire the mission_contexts field into RunState.

Purpose: This is the foundation for the entire phase -- the typed context model that all eviction, enrichment, and wiring tasks depend on.
Output: context_manager.py with models + ContextManager skeleton, updated state_schema.py, unit tests.
</objective>

<execution_context>
@/home/nir/.claude/get-shit-done/workflows/execute-plan.md
@/home/nir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-context-manipulation-for-better-sub-agent-multi-task-handling/07.1-CONTEXT.md
@.planning/phases/07.1-context-manipulation-for-better-sub-agent-multi-task-handling/07.1-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/agentic_workflows/orchestration/langgraph/state_schema.py:
```python
class RunState(TypedDict):
    # ... existing fields ...
    # NEW: mission_contexts: dict[str, Any]  -- add this field
    pass

class MissionReport(TypedDict):
    mission_id: int
    mission: str
    used_tools: list[str]
    tool_results: list[dict[str, Any]]
    result: str
    status: Literal["pending", "in_progress", "completed", "failed"]
    required_tools: list[str]
    required_files: list[str]
    written_files: list[str]
    expected_fibonacci_count: int | None
    contract_checks: list[str]
    subtask_contracts: list[dict[str, Any]]
    subtask_statuses: list[dict[str, Any]]

def new_run_state(system_prompt: str, user_input: str, run_id: str | None = None) -> RunState:
    # Add "mission_contexts": {} to the return dict

def ensure_state_defaults(state, *, system_prompt: str = "") -> RunState:
    # Add: if "mission_contexts" not in state_dict: state_dict["mission_contexts"] = {}
```

From src/agentic_workflows/orchestration/langgraph/mission_parser.py:
```python
class MissionStep:  # has parent_id for sub-mission hierarchy
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MissionContext model + artifact extraction + summary generation</name>
  <files>src/agentic_workflows/orchestration/langgraph/context_manager.py, tests/unit/test_context_manager.py</files>
  <behavior>
    - test_mission_context_model: MissionContext can be created with mission_id, goal, status; serializes via model_dump(); validates via model_validate()
    - test_sub_mission_context: SubMissionContext nests inside MissionContext.sub_missions dict
    - test_artifact_record: ArtifactRecord holds key/value/source_tool/source_mission_id
    - test_mission_summary: MissionContext.build_summary() produces deterministic string with goal, tools, key_results, artifacts
    - test_mission_summary_empty: build_summary() works with empty tools/results/artifacts
    - test_artifact_extraction_write_file: extract_artifacts("write_file", result, args) returns ArtifactRecord with file_path
    - test_artifact_extraction_data_analysis: extract_artifacts("data_analysis", result, args) returns mean/outlier artifacts
    - test_artifact_extraction_sort_array: extract_artifacts("sort_array", result, args) returns sorted_result artifact
    - test_artifact_extraction_error: extract_artifacts for any tool with error in result returns empty list
    - test_artifact_extraction_unknown_tool: extract_artifacts for unknown tool returns generic artifact
    - test_extract_summary_from_result_write_file: extract_summary_from_result returns outcome field
    - test_extract_summary_from_result_data_analysis: extract_summary_from_result returns mean/median/etc
    - test_extract_summary_from_result_generic: extract_summary_from_result returns truncated result for unknown tools
    - test_cross_mission_artifacts: ContextManager.get_artifacts_for_mission() returns artifacts from prior missions only
  </behavior>
  <action>
    Create `src/agentic_workflows/orchestration/langgraph/context_manager.py` with:

    1. **ArtifactRecord** Pydantic BaseModel: key (str), value (str), source_tool (str), source_mission_id (int)
    2. **SubMissionContext** Pydantic BaseModel: sub_mission_id (str), goal (str), status (str, default "pending"), tools_used (list[str]), key_results (dict[str, str])
    3. **MissionContext** Pydantic BaseModel: mission_id (int), goal (str), status (str, default "pending"), tools_used (list[str]), key_results (dict[str, str]), artifacts (list[ArtifactRecord]), sub_missions (dict[str, SubMissionContext]), summary (str, default ""), step_range (tuple[int, int] | None, default None) for tracking message boundaries by step number (not message indices -- per RESEARCH.md pitfall 3)
    4. **MissionContext.build_summary()** method: deterministic summary from structured data. Format: "Mission {id}: {goal} | Tools: {tool1 -> tool2} | {key}: {value} | Artifacts: {key=value, ...}"
    5. **extract_artifacts(tool_name, result, args, mission_id)** function: uses _ARTIFACT_EXTRACTORS dict. write_file extracts file_path from args.get("path"). data_analysis extracts mean, outliers. sort_array extracts sorted_result. Unknown tools extract first 200 chars of stringified result as "result" key. Error results return empty list.
    6. **extract_summary_from_result(tool_name, result)** function: deterministic key field extraction per RESEARCH.md pattern. Returns dict[str, str].
    7. **ContextManager** class skeleton with __init__(large_result_threshold=4000, sliding_window_cap=30, step_threshold=10) and get_artifacts_for_mission(state, mission_id) method that returns artifacts from all MissionContexts with mission_id < the requested one.

    Store MissionContext as serialized dicts (model_dump()) in RunState, reconstruct via model_validate() when reading -- per RESEARCH.md pitfall 2 guidance.

    Create `tests/unit/test_context_manager.py` with RED tests first, then GREEN implementation.
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && python -m pytest tests/unit/test_context_manager.py -x -q</automated>
  </verify>
  <done>MissionContext, ArtifactRecord, SubMissionContext models exist with build_summary(), extract_artifacts(), extract_summary_from_result(), and ContextManager.get_artifacts_for_mission(). All 14 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire mission_contexts into RunState</name>
  <files>src/agentic_workflows/orchestration/langgraph/state_schema.py</files>
  <action>
    Add `mission_contexts: dict[str, Any]` field to RunState TypedDict. Use dict[str, Any] (not dict[str, MissionContext]) because Pydantic BaseModel objects in TypedDict cause checkpointer serialization issues -- per RESEARCH.md pitfall 2.

    String keys (str(mission_id)) for JSON serialization safety -- per RESEARCH.md recommendation.

    In `new_run_state()`: add `"mission_contexts": {}` to the return dict.

    In `ensure_state_defaults()`: add `if "mission_contexts" not in state_dict: state_dict["mission_contexts"] = {}` after the existing field defaults.

    Do NOT add a custom reducer -- parallel Send() is deferred (PRLL-01 out of scope). Plain dict replacement semantics are safe for sequential execution.
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && python -c "from agentic_workflows.orchestration.langgraph.state_schema import new_run_state; s = new_run_state('sys', 'hi'); assert 'mission_contexts' in s and s['mission_contexts'] == {}, f'missing or wrong: {s.get(\"mission_contexts\")}'; print('OK')"</automated>
  </verify>
  <done>RunState has mission_contexts field, new_run_state() initializes it to {}, ensure_state_defaults() defaults it to {}.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/test_context_manager.py -x -q` -- all model + extraction + summary tests pass
- `python -c "from agentic_workflows.orchestration.langgraph.context_manager import MissionContext, ContextManager"` -- imports work
- `python -c "from agentic_workflows.orchestration.langgraph.state_schema import new_run_state; assert 'mission_contexts' in new_run_state('s','h')"` -- field exists
</verification>

<success_criteria>
- MissionContext model creates, serializes (model_dump), validates (model_validate) correctly
- build_summary() produces deterministic string with all mission data
- extract_artifacts() handles write_file, data_analysis, sort_array, unknown tools, and error results
- extract_summary_from_result() extracts key fields per tool type
- ContextManager.get_artifacts_for_mission() returns only prior-mission artifacts
- RunState.mission_contexts field exists and defaults to {}
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-context-manipulation-for-better-sub-agent-multi-task-handling/07.1-01-SUMMARY.md`
</output>
