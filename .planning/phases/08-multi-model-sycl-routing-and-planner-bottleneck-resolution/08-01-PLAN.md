---
phase: 08-multi-model-sycl-routing-and-planner-bottleneck-resolution
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_provider_port.py
  - tests/unit/test_tool_result_cache.py
  - tests/integration/test_context_overflow.py
autonomous: true
requirements:
  - SYCL-01
  - SYCL-02
  - BTLNK-01
  - BTLNK-02

must_haves:
  truths:
    - "Failing test stubs exist for with_port() factory and role-specific provider routing"
    - "Failing test stubs exist for ToolResultCache (store, get, TTL eviction, pool=None)"
    - "Failing test stub exists for large-result context cap integration scenario"
    - "All new stubs fail at collection time with NotImplementedError (RED state confirmed)"
  artifacts:
    - path: "tests/unit/test_provider_port.py"
      provides: "Test stubs for SYCL-01 with_port() and orchestrator port env var wiring"
    - path: "tests/unit/test_tool_result_cache.py"
      provides: "Test stubs for BTLNK-02 ToolResultCache store/get/TTL/pool-none"
    - path: "tests/integration/test_context_overflow.py"
      provides: "Test stub for BTLNK-01 large-result planner context cap"
  key_links:
    - from: "tests/unit/test_provider_port.py"
      to: "src/agentic_workflows/orchestration/langgraph/provider.py"
      via: "imports LlamaCppChatProvider"
      pattern: "LlamaCppChatProvider"
    - from: "tests/unit/test_tool_result_cache.py"
      to: "src/agentic_workflows/storage/tool_result_cache.py"
      via: "imports ToolResultCache"
      pattern: "ToolResultCache"
---

<objective>
Create Wave 0 failing test stubs for all new Phase 8 behaviors.

Purpose: Establish RED state before any implementation so tests drive development. Per project convention (Phase 7.6-00 decision), stubs use NotImplementedError — not pytest.skip — to guarantee genuine failure at collection time.
Output: Three test files covering SYCL-01 (with_port factory), BTLNK-02 (ToolResultCache), and BTLNK-01 (context overflow cap).
</objective>

<execution_context>
@/home/nir/.claude/get-shit-done/workflows/execute-plan.md
@/home/nir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-multi-model-sycl-routing-and-planner-bottleneck-resolution/08-CONTEXT.md
@.planning/phases/08-multi-model-sycl-routing-and-planner-bottleneck-resolution/08-RESEARCH.md

<interfaces>
<!-- Key existing patterns the executor needs for test stubs. -->

From src/agentic_workflows/orchestration/langgraph/provider.py:
```python
# with_alias() pattern at line 568 — with_port() will follow same __new__-clone structure
class LlamaCppChatProvider:
    def with_alias(self, alias: str) -> LlamaCppChatProvider: ...
    # with_port(port: int) -> LlamaCppChatProvider  ← to be added in Plan 02
```

From src/agentic_workflows/storage/artifact_store.py (template for ToolResultCache):
```python
class ArtifactStore:
    def __init__(self, pool: ConnectionPool | None = None) -> None: ...
    # ToolResultCache follows same pattern with store()/get() methods
```

From tests/unit/test_structural_health.py (Wave 0 stub convention):
```python
def test_something():
    raise NotImplementedError("stub — implement in Plan NN")
```

From tests/conftest.py — existing fixtures available:
- ScriptedProvider
- pg_pool (requires psycopg_pool — use pytest.importorskip)
- clean_pg
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_provider_port.py stubs for SYCL-01</name>
  <files>tests/unit/test_provider_port.py</files>
  <behavior>
    - test_with_port_returns_new_provider: with_port(9090) returns a different object, not self
    - test_with_port_url_contains_new_port: new provider's client base_url contains port 9090
    - test_with_port_preserves_model_and_settings: model name and timeout_seconds copied to clone
    - test_with_port_composes_with_with_alias: provider.with_alias("planner").with_port(8081) returns provider pointing to 8081
    - test_orchestrator_reads_planner_port_env: when LLAMA_CPP_PLANNER_PORT=9090 set, orchestrator._planner_provider URL has port 9090
    - test_orchestrator_reads_executor_port_env: when LLAMA_CPP_EXECUTOR_PORT=9091 set, orchestrator._executor_provider URL has port 9091
    - test_orchestrator_fallback_when_no_port_envs: without port env vars, _planner_provider and _executor_provider are the same object as provider
    - test_orchestrator_warn_on_unreachable_port: when configured port server is unreachable, logs warning and falls back (no hard fail)
  </behavior>
  <action>
    Create tests/unit/test_provider_port.py with 8 test stubs. Each test body raises NotImplementedError("stub — implement in Plan 02").

    Module-level imports (wrapped in try/except at module level to prevent collection failure if the module doesn't exist yet):
    ```python
    import pytest
    try:
        from agentic_workflows.orchestration.langgraph.provider import LlamaCppChatProvider
    except ImportError:
        LlamaCppChatProvider = None  # type: ignore
    ```

    Do NOT use pytest.importorskip for provider — it already exists. The with_port() method does not exist yet; tests import the class and call the nonexistent method inside the test body (that's what triggers NotImplementedError on the stub).

    For orchestrator tests: import LangGraphOrchestrator inside the test function body (lazy import per project pattern in existing tests).
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && pytest tests/unit/test_provider_port.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>All 8 tests collected; all fail with NotImplementedError (not ImportError or collection error)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_tool_result_cache.py and test_context_overflow.py stubs for BTLNK-01/02</name>
  <files>tests/unit/test_tool_result_cache.py, tests/integration/test_context_overflow.py</files>
  <behavior>
    test_tool_result_cache.py stubs:
    - test_store_and_get_round_trip: store result → get by same key returns full result
    - test_get_returns_none_on_miss: get() for unknown key returns None
    - test_get_deletes_expired_and_returns_none: store with expires_at in the past → get() returns None
    - test_pool_none_store_is_noop: ToolResultCache(pool=None).store(...) does not raise
    - test_pool_none_get_returns_none: ToolResultCache(pool=None).get(...) returns None
    - test_args_hash_is_stable: same tool_name + args always produce same cache key
    - test_structural_health_truncations_increments: after ContextManager intercepts large result, structural_health["tool_result_truncations"] == 1

    test_context_overflow.py stubs (integration, Postgres required):
    - test_large_result_never_reaches_planner_raw: after tool returns >2000 chars, planner injection contains compact pointer not raw result
    - test_full_result_retrievable_from_cache: after truncation, ToolResultCache.get() returns the original full result
  </action>
  <action>
    Create tests/unit/test_tool_result_cache.py with 7 stubs. Each raises NotImplementedError("stub — implement in Plan 03/04").

    At the top of test_tool_result_cache.py, wrap ToolResultCache import in try/except (class doesn't exist yet):
    ```python
    try:
        from agentic_workflows.storage.tool_result_cache import ToolResultCache
    except ImportError:
        ToolResultCache = None  # type: ignore
    ```

    For the structural_health stub, lazy-import ContextManager + LangGraphOrchestrator inside test body.

    Create tests/integration/test_context_overflow.py with 2 stubs. Use pytest.importorskip("psycopg_pool") at module level (project-standard Postgres test pattern per Phase 7 decisions). Each stub body raises NotImplementedError("stub — implement in Plan 05").
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && pytest tests/unit/test_tool_result_cache.py tests/integration/test_context_overflow.py -q 2>&1 | tail -8</automated>
  </verify>
  <done>All 9 stubs collected (7 unit + 2 integration); all fail with NotImplementedError; no ImportError or collection error; full suite still passes: pytest tests/ -q passes all 823+ existing tests</done>
</task>

</tasks>

<verification>
- pytest tests/unit/test_provider_port.py -q — 8 tests, all NotImplementedError
- pytest tests/unit/test_tool_result_cache.py -q — 7 tests, all NotImplementedError
- pytest tests/integration/test_context_overflow.py -q — 2 tests, all NotImplementedError or skipped (psycopg_pool not installed in CI)
- pytest tests/ -q — existing 823+ tests still green (no regression from adding stub files)
</verification>

<success_criteria>
- 17 new test stubs exist across 3 files
- All stubs are in RED state (NotImplementedError, not skip)
- Zero regressions in existing test suite
- Test files are importable without errors (lazy imports inside test functions for unresolved symbols)
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-model-sycl-routing-and-planner-bottleneck-resolution/08-01-SUMMARY.md`
</output>
