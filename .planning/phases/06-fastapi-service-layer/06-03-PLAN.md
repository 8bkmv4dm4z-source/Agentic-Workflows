---
phase: 06-fastapi-service-layer
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/agentic_workflows/cli/__init__.py
  - src/agentic_workflows/cli/user_run.py
  - src/agentic_workflows/orchestration/langgraph/user_run.py
  - tests/eval/__init__.py
  - tests/eval/conftest.py
  - tests/eval/test_eval_harness.py
autonomous: false
requirements:
  - PROD-01
  - PROD-02

must_haves:
  truths:
    - "user_run.py talks to FastAPI service over HTTP, not orchestrator directly"
    - "user_run.py renders SSE events with Rich terminal output during streaming"
    - "user_run.py auto-starts uvicorn server if not running"
    - "Eval harness runs 3+ deterministic ScriptedProvider scenarios through the API"
    - "All existing tests continue to pass"
  artifacts:
    - path: "src/agentic_workflows/cli/user_run.py"
      provides: "API client user_run with Rich SSE rendering and auto-start"
      min_lines: 80
      contains: "httpx"
    - path: "tests/eval/test_eval_harness.py"
      provides: "3+ eval scenarios using ScriptedProvider through API"
      min_lines: 50
  key_links:
    - from: "src/agentic_workflows/cli/user_run.py"
      to: "src/agentic_workflows/api/app.py"
      via: "httpx POST /run with SSE streaming"
      pattern: "client\\.stream.*POST.*\\/run"
    - from: "tests/eval/test_eval_harness.py"
      to: "src/agentic_workflows/api/app.py"
      via: "httpx AsyncClient with ASGITransport for eval scenarios"
      pattern: "ASGITransport"
---

<objective>
Convert user_run.py to API client with Rich SSE rendering, create eval harness, and verify end-to-end

Purpose: Make user_run.py the CLI interface that talks to the FastAPI service (not the orchestrator directly), fulfilling the "single source of truth" constraint. Add eval harness with 3+ deterministic scenarios for CI-safe regression testing. Final verification checkpoint confirms the full stack works.

Output: user_run.py as API client, eval harness with passing scenarios, human-verified end-to-end flow.
</objective>

<execution_context>
@/home/nir/.claude/get-shit-done/workflows/execute-plan.md
@/home/nir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-fastapi-service-layer/06-CONTEXT.md
@.planning/phases/06-fastapi-service-layer/06-RESEARCH.md
@.planning/phases/06-fastapi-service-layer/06-01-SUMMARY.md
@.planning/phases/06-fastapi-service-layer/06-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 01 and 02 -->

From src/agentic_workflows/api/models.py:
```python
class RunRequest(BaseModel):
    user_input: str
    run_id: str | None = None
    prior_context: list[dict[str, Any]] | None = None

class HealthResponse(BaseModel):
    status: str
    provider: str
    tool_count: int
```

SSE event format (from POST /run response):
```
event: node_start
data: {"type": "node_start", "node": "plan", "run_id": "...", "tier": "ui", "timestamp": "..."}

event: node_end
data: {"type": "node_end", "node": "plan", "run_id": "...", "tier": "ui", "timestamp": "..."}

event: run_complete
data: {"type": "run_complete", "run_id": "...", "tier": "ui", "timestamp": "..."}
```

From tests/conftest.py:
```python
# ScriptedProvider fixture available for deterministic testing
# api_client fixture provides httpx AsyncClient against test app
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert user_run.py to API client with Rich SSE rendering and auto-start</name>
  <files>
    src/agentic_workflows/cli/__init__.py,
    src/agentic_workflows/cli/user_run.py,
    src/agentic_workflows/orchestration/langgraph/user_run.py
  </files>
  <action>
1. **src/agentic_workflows/cli/__init__.py** — Empty init.

2. **src/agentic_workflows/cli/user_run.py** — New API client version:
   - Read `API_BASE_URL` from env (default `http://localhost:8000`).
   - `_ensure_server_running()`: Try `httpx.get(f"{API_BASE}/health", timeout=2)`. If fails, spawn uvicorn in background via `subprocess.Popen([sys.executable, "-m", "uvicorn", "agentic_workflows.api.app:app", "--host", "0.0.0.0", "--port", "8000", "--log-level", "warning"], start_new_session=True)`. Retry health check up to 5 times with 1s sleep. Raise if still failing.
   - `async def stream_run(user_input: str, prior_context: list[dict] | None = None) -> str`:
     - Use `httpx.AsyncClient(base_url=API_BASE, timeout=300)`.
     - `async with client.stream("POST", "/run", json=RunRequest(user_input=user_input, prior_context=prior_context).model_dump(exclude_none=True)) as resp:`
     - Parse SSE lines: for each `data: {...}` line, json.loads and dispatch to Rich rendering.
     - Extract run_id from events for return value.
   - Rich rendering via `rich.console.Console` and `rich.panel.Panel`:
     - node_start: `console.print(f"[bold blue]>>> {node}[/]")`
     - node_end: `console.print(f"[green]  completed: {node}[/]")`
     - run_complete: `console.print(Panel("Run Complete", style="bold green"))`
     - state_diff (debug tier): only print if `--debug` flag or `DEBUG_SSE` env var is set
     - error: `console.print(f"[bold red]ERROR: {detail}[/]")`
   - `async def interactive_session()`: Loop prompting user for input, call stream_run for each, accumulate prior_context from conversation history. Ctrl+C exits gracefully.
   - `def main()`: Parse args (optional `--debug` flag), call `_ensure_server_running()`, run `asyncio.run(interactive_session())`.
   - `if __name__ == "__main__": main()`

3. **src/agentic_workflows/orchestration/langgraph/user_run.py** — Add a deprecation notice at the top:
   ```python
   """DEPRECATED: Use `python -m agentic_workflows.cli.user_run` instead.
   This module is retained for backward compatibility."""
   import warnings
   warnings.warn("user_run.py has moved to agentic_workflows.cli.user_run", DeprecationWarning, stacklevel=2)
   ```
   Keep existing code functional but add the warning. Do NOT delete — backward compat.
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && python -c "from agentic_workflows.cli.user_run import main, _ensure_server_running, stream_run; print('CLI user_run imports OK')"</automated>
  </verify>
  <done>user_run.py in cli/ package talks to FastAPI service via httpx, renders SSE events with Rich, auto-starts uvicorn if not running. Old user_run.py shows deprecation warning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create eval harness with 3+ ScriptedProvider scenarios through the API</name>
  <files>
    tests/eval/__init__.py,
    tests/eval/conftest.py,
    tests/eval/test_eval_harness.py
  </files>
  <behavior>
    - test_eval_simple_mission: Single mission "write hello to a file" completes with run_complete SSE event and GET /run/{id} shows status="completed"
    - test_eval_multi_mission: Multi-mission workload with 2+ missions all produce mission_reports
    - test_eval_tool_chain: Mission requiring chained tool calls (e.g., data_analysis -> sort_array) produces correct tool_history ordering
    - All scenarios use ScriptedProvider — zero live LLM calls
  </behavior>
  <action>
1. **tests/eval/__init__.py** — Empty init.

2. **tests/eval/conftest.py** — Eval-specific fixtures:
   - `eval_app` fixture: FastAPI app configured with ScriptedProvider (reuse or extend the api_client pattern from conftest.py). Use tmp_path for RunStore.
   - `eval_client` fixture: httpx AsyncClient with ASGITransport against eval_app.
   - Define 3+ ScriptedProvider response sequences for deterministic scenarios:
     - `simple_mission_responses`: Single mission, one tool call, finish action.
     - `multi_mission_responses`: Two missions, each with tool calls and finish.
     - `tool_chain_responses`: data_analysis -> sort_array chain with correct action sequence.
   - Each response sequence provides exactly the JSON action responses the LLM would produce, matching the ToolAction/FinishAction Pydantic contracts.

3. **tests/eval/test_eval_harness.py** — Eval scenarios:
   - `@pytest.mark.asyncio` on all tests.
   - **test_eval_simple_mission**: POST /run with simple input, parse SSE events, verify run_complete arrives. Then GET /run/{id} and verify status="completed", len(mission_reports) == 1.
   - **test_eval_multi_mission**: POST /run with multi-mission input, verify all mission_reports present, no dropped results.
   - **test_eval_tool_chain**: POST /run with chain-dependent input, verify tool_history shows correct ordering (data_analysis before sort_array).
   - Each test asserts on concrete values from the ScriptedProvider responses — deterministic, not fuzzy.
   - Parse SSE from response body (httpx streaming or full-body read + line parsing).
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && pytest tests/eval/ -x -q</automated>
  </verify>
  <done>3 eval scenarios pass through the API using ScriptedProvider. Zero live LLM calls. Eval harness is CI-safe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 6 FastAPI service end-to-end</name>
  <action>Human verifies the full Phase 6 stack works: API server starts, all endpoints respond correctly, SSE streaming works, user_run CLI connects, and full test suite passes.</action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && pytest tests/ -q</automated>
  </verify>
  <done>Human confirms API server starts with graph compiled once, all endpoints work via curl, SSE events stream incrementally, user_run CLI connects and renders Rich output, full test suite passes.</done>
  <what-built>
Complete Phase 6 FastAPI service layer:
- POST /run with SSE streaming of node transitions
- GET /run/{id} with completed/in-progress results
- GET /run/{id}/stream for reconnection
- GET /health and GET /tools for introspection
- SQLiteRunStore with WAL mode for concurrent access
- user_run.py converted to API client with Rich rendering
- Eval harness with 3+ deterministic scenarios
  </what-built>
  <how-to-verify>
1. Start the API server: `python -m agentic_workflows.api.app`
   - Verify log line shows "graph_compiled" at startup
   - Verify it starts without errors

2. Test health endpoint: `curl http://localhost:8000/health`
   - Should return {"status": "ok", "provider": "...", "tool_count": N}

3. Test tools endpoint: `curl http://localhost:8000/tools`
   - Should return list of tool objects with name and description

4. Test run submission: `curl -N -X POST http://localhost:8000/run -H "Content-Type: application/json" -d '{"user_input": "Write hello world to a file called test.txt"}'`
   - Should stream SSE events (node_start, node_end, run_complete)
   - Events should arrive incrementally, not all at once

5. Test run retrieval: `curl http://localhost:8000/run/{run_id}` (use run_id from step 4)
   - Should return completed status with result

6. Run full test suite: `pytest tests/ -q`
   - All tests should pass (429+ existing + new API/eval tests)

7. (Optional) Test user_run CLI: `python -m agentic_workflows.cli.user_run`
   - Should auto-start server if not running
   - Type a mission and verify Rich-formatted SSE output
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pytest tests/eval/ -x -q` — all eval scenarios pass
- `pytest tests/ -q` — full suite green
- `curl http://localhost:8000/health` returns 200 (when server running)
- `python -m agentic_workflows.cli.user_run` starts and connects to API
</verification>

<success_criteria>
- user_run.py talks to FastAPI service via httpx, not orchestrator directly
- Rich terminal output renders SSE events during streaming
- Auto-start spawns uvicorn if API is not running
- 3+ eval scenarios pass through API using ScriptedProvider
- Full test suite passes (existing + new)
- Human verifies end-to-end flow works with curl and/or user_run CLI
</success_criteria>

<output>
After completion, create `.planning/phases/06-fastapi-service-layer/06-03-SUMMARY.md`
</output>
