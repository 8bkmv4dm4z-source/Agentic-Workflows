---
phase: 07.8-multi-model-provider-routing-+-smart-cloud-fallback
plan: 02
subsystem: orchestration
tags: [model-routing, typeddict, threshold-routing, signal-based]

# Dependency graph
requires:
  - phase: 07.7-hybrid-intent-classifier-and-few-shot-prompts
    provides: "IntentClassification and route_by_intent() on ModelRouter"
provides:
  - "RoutingSignals TypedDict with 5 runtime signal fields"
  - "route_by_signals() threshold-based routing method on ModelRouter"
  - "_BUDGET_STRONG_THRESHOLD and _RETRY_STRONG_THRESHOLD module constants"
  - "Deprecated route_by_intent() backward-compat shim"
affects: [07.8-03-graph-wiring, 07.8-04-cloud-fallback]

# Tech tracking
tech-stack:
  added: []
  patterns: [signal-based-routing, threshold-cascade, deprecation-shim]

key-files:
  modified:
    - src/agentic_workflows/orchestration/langgraph/model_router.py
    - tests/unit/test_model_router_wiring.py

key-decisions:
  - "Kept route_by_intent() as deprecated shim with DeprecationWarning instead of removing -- graph.py caller migrates in Plan 03"
  - "Budget threshold uses strict < (not <=) so exactly 5000 routes fast"
  - "Retry threshold uses >= 2 (two or more retries escalate to strong)"

patterns-established:
  - "RoutingSignals TypedDict: standard signal bag for model selection decisions"
  - "Threshold cascade priority: retry > budget > mission_type > intent > default"

requirements-completed: [SC-2]

# Metrics
duration: 5min
completed: 2026-03-10
---

# Phase 07.8 Plan 02: Signal-Based Routing Summary

**RoutingSignals TypedDict + route_by_signals() with threshold cascade replacing intent-only routing on ModelRouter**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-10T15:45:39Z
- **Completed:** 2026-03-10T15:50:41Z
- **Tasks:** 1 (TDD: RED + GREEN)
- **Files modified:** 2

## Accomplishments
- Added RoutingSignals TypedDict with token_budget_remaining, mission_type, retry_count, step, intent_classification
- Implemented route_by_signals() with 6-level threshold cascade: retry >= 2 -> strong, budget < 5000 -> strong, multi_step -> strong, complex intent -> strong, simple intent -> fast, default -> fast
- 11 parametrized test cases covering all routing paths including boundary conditions
- Backward-compat route_by_intent() shim emitting DeprecationWarning for graph.py caller (migrated in Plan 03)

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace route_by_intent with route_by_signals + RoutingSignals TypedDict** - `b1ae2f1` (feat, TDD)

_Note: TDD task with RED (failing tests) then GREEN (implementation) in single commit_

## Files Created/Modified
- `src/agentic_workflows/orchestration/langgraph/model_router.py` - RoutingSignals TypedDict, route_by_signals(), deprecated route_by_intent() shim, threshold constants
- `tests/unit/test_model_router_wiring.py` - 22 tests total: 6 original route() tests, 11 parametrized route_by_signals tests, 1 dual-provider test, 4 deprecated route_by_intent backward-compat tests

## Decisions Made
- Kept route_by_intent() as deprecated shim (with DeprecationWarning) instead of removing it outright -- graph.py:1696 still calls it and will be migrated in Plan 03
- Budget threshold boundary: exactly 5000 routes fast (strict less-than), ensuring predictable boundary behavior
- RoutingSignals uses TypedDict (not Pydantic BaseModel) for zero-overhead signal passing -- no validation needed at routing hot path

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Kept route_by_intent() as deprecated shim instead of removing**
- **Found during:** Task 1 (GREEN phase verification)
- **Issue:** Plan said "route_by_intent() removed" but graph.py:1696 calls it; removing it broke 31 integration tests
- **Fix:** Added deprecated shim that delegates to route_by_signals() for intent cases and route() for fallback cases, with DeprecationWarning
- **Files modified:** model_router.py, test_model_router_wiring.py
- **Verification:** All 22 router tests pass; 31 pre-existing test failures confirmed unrelated (same failures on stashed original code)
- **Committed in:** b1ae2f1

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to avoid breaking existing callers. Plan 03 will complete the migration and remove the shim.

## Issues Encountered
- 31 pre-existing test failures in integration suite (test_langgraph_flow, test_multi_mission_subgraph, test_action_queue, test_structural_health) -- confirmed pre-existing via stash test, not caused by this plan's changes

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- RoutingSignals and route_by_signals() ready for Plan 03 to wire into graph.py _plan_next_action()
- route_by_intent() deprecation shim ensures zero breakage during migration window

---
*Phase: 07.8-multi-model-provider-routing-+-smart-cloud-fallback*
*Completed: 2026-03-10*
