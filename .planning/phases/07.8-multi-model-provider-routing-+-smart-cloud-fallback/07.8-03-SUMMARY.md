---
phase: 07.8-multi-model-provider-routing-+-smart-cloud-fallback
plan: 03
subsystem: orchestration
tags: [cloud-fallback, routing-signals, alias-wiring, structural-health]

# Dependency graph
requires:
  - phase: 07.8-01
    provides: "LlamaCppChatProvider.with_alias() factory method"
  - phase: 07.8-02
    provides: "RoutingSignals TypedDict and route_by_signals() on ModelRouter"
provides:
  - "Cloud fallback on ProviderTimeoutError (tries fallback_provider before deterministic)"
  - "Cloud fallback on 2+ consecutive parse failures"
  - "Signal-based routing wired into graph.py _plan_next_action"
  - "Alias-based dual providers from LLAMA_CPP_STRONG_ALIAS / LLAMA_CPP_FAST_ALIAS env vars"
  - "structural_health gains cloud_fallback_count, local_model_failures, routing_decisions"
affects: [07.8-04-walkthrough]

# Tech tracking
tech-stack:
  added: []
  patterns: [cloud-fallback-before-deterministic, per-step-fallback, routing-signal-construction]

key-files:
  modified:
    - src/agentic_workflows/orchestration/langgraph/graph.py
    - src/agentic_workflows/orchestration/langgraph/state_schema.py
    - tests/integration/test_model_router_integration.py
    - tests/unit/test_structural_health.py

key-decisions:
  - "Cloud fallback is per-step (not sticky) -- each timeout/parse-failure tries fallback independently"
  - "Routing decision tracking calls route_by_signals twice (once for tracking, once in _generate_with_hard_timeout) -- acceptable since route_by_signals is pure logic with no side effects"
  - "Consecutive parse failures tracked on instance (_consecutive_parse_failures) not state -- resets per orchestrator lifecycle, not per step"

patterns-established:
  - "Cloud fallback pattern: try fallback_provider.generate() before deterministic fallback on timeout/parse failures"
  - "RoutingSignals construction: build from state fields (token_budget, retry_count, step, intent) at call site"

requirements-completed: [SC-3, SC-4, SC-6]

# Metrics
duration: 8min
completed: 2026-03-10
---

# Phase 07.8 Plan 03: Graph Wiring + Cloud Fallback Summary

**Cloud fallback on timeout/parse failures, route_by_signals wiring, alias-based dual providers, and routing decision tracking in structural_health**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-10T15:53:02Z
- **Completed:** 2026-03-10T16:01:02Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- Wired route_by_signals() replacing deprecated route_by_intent() in graph.py
- Added cloud fallback logic on ProviderTimeoutError and consecutive parse failures
- Added fallback_provider parameter and alias-based dual providers from env vars
- Expanded structural_health with cloud_fallback_count, local_model_failures, routing_decisions
- 4 new integration tests covering all cloud fallback scenarios

## Task Commits

Each task was committed atomically:

1. **Task 1: State defaults + __init__ wiring** - `f478110` (feat)
2. **Task 2: Cloud fallback logic + route_by_signals wiring** - `58fa5b0` (feat)
3. **Task 3: Integration tests for cloud fallback** - `803c1db` (test)

## Files Created/Modified
- `src/agentic_workflows/orchestration/langgraph/state_schema.py` - Added cloud_fallback_count, local_model_failures, routing_decisions to structural_health defaults
- `src/agentic_workflows/orchestration/langgraph/graph.py` - fallback_provider param, alias wiring, route_by_signals, cloud fallback on timeout + parse failures
- `tests/integration/test_model_router_integration.py` - 4 new cloud fallback integration tests
- `tests/unit/test_structural_health.py` - Updated assertions for new structural_health keys

## Decisions Made
- Cloud fallback is per-step (not sticky across steps) -- each timeout/parse-failure independently attempts the fallback provider
- route_by_signals called twice at call site (once for tracking, once inside _generate_with_hard_timeout) -- pure logic function, no side effects
- _consecutive_parse_failures is an instance attribute (not state field) -- resets naturally per orchestrator lifecycle

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Missing return statement in _generate_with_hard_timeout**
- **Found during:** Task 2
- **Issue:** When replacing the old _generate_with_hard_timeout, the `return str(payload)` line was dropped, causing the function to return None on success
- **Fix:** Added `return str(payload)` after the error-handling block
- **Files modified:** graph.py
- **Verification:** test_json_parse_fallback_counter_increments passes
- **Committed in:** 58fa5b0

**2. [Rule 1 - Bug] test_structural_health exact dict assertion too strict**
- **Found during:** Task 2
- **Issue:** test_clean_run_has_zero_counters used `sh == {"json_parse_fallback": 0, "schema_mismatch": 0}` which fails with new keys
- **Fix:** Changed to per-key assertions checking each counter is 0
- **Files modified:** tests/unit/test_structural_health.py
- **Verification:** All 4 structural_health tests pass
- **Committed in:** 58fa5b0

---

**Total deviations:** 2 auto-fixed (2 bugs)
**Impact on plan:** Both auto-fixes necessary for correctness. No scope creep.

## Issues Encountered
- 28 pre-existing integration test failures in test_langgraph_flow.py confirmed unrelated (same failures on stashed code)
- 1 pre-existing unit test failure in test_action_queue.py (test_multi_action_queued_and_popped) confirmed unrelated

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Cloud fallback fully wired -- ready for Plan 04 (walkthrough/documentation)
- route_by_intent() deprecated shim can now be removed (all callers migrated)

---
*Phase: 07.8-multi-model-provider-routing-+-smart-cloud-fallback*
*Completed: 2026-03-10*

## Self-Check: PASSED
