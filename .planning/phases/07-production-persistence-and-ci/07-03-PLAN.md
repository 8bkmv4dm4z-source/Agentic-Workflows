---
phase: 07-production-persistence-and-ci
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - Dockerfile
  - docker-compose.yml
  - .dockerignore
  - Makefile
  - .github/workflows/ci.yml
  - pyproject.toml
autonomous: false
requirements: [PROD-04, PROD-05]

must_haves:
  truths:
    - "docker build succeeds and produces a runnable image"
    - "docker-compose up starts both Postgres and FastAPI containers"
    - "FastAPI container depends on Postgres health check before starting"
    - "Postgres data persists across container restarts via named volume"
    - "GitHub Actions CI runs ruff, mypy, pytest against both SQLite and Postgres backends"
    - "CI runs Docker build test (build only, no push)"
    - "pytest-cov enforces 80% coverage threshold in CI"
    - "No live LLM credentials required in CI (ScriptedProvider only)"
  artifacts:
    - path: "Dockerfile"
      provides: "Single-stage python:3.12-slim build for FastAPI service"
      contains: "uvicorn"
    - path: "docker-compose.yml"
      provides: "Postgres + FastAPI service composition with health checks"
      contains: "pgvector/pgvector:pg16"
    - path: ".dockerignore"
      provides: "Excludes .venv, .git, .tmp, __pycache__ from Docker context"
      min_lines: 5
    - path: ".github/workflows/ci.yml"
      provides: "CI with Postgres service, test matrix, Docker build, coverage"
      contains: "matrix"
  key_links:
    - from: "docker-compose.yml"
      to: "db/migrations/"
      via: "volume mount to /docker-entrypoint-initdb.d/"
      pattern: "docker-entrypoint-initdb.d"
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "build context for api service"
      pattern: "build: ."
    - from: ".github/workflows/ci.yml"
      to: "pgvector/pgvector:pg16"
      via: "Postgres service container for test matrix"
      pattern: "pgvector"
    - from: ".github/workflows/ci.yml"
      to: "pytest-cov"
      via: "--cov-fail-under=80 flag"
      pattern: "cov-fail-under"
---

<objective>
Create Docker containerization (Dockerfile + docker-compose.yml) for the full system and extend the CI pipeline with Postgres service containers, test matrix, Docker build test, and coverage enforcement.

Purpose: PROD-04 delivers one-command deployment (`docker-compose up`). PROD-05 delivers a hardened CI pipeline that catches regressions against both SQLite and Postgres backends. Together they complete the production infrastructure for v1.

Output: Dockerfile, docker-compose.yml, .dockerignore, updated Makefile, updated CI workflow.
</objective>

<execution_context>
@/home/nir/.claude/get-shit-done/workflows/execute-plan.md
@/home/nir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-production-persistence-and-ci/07-CONTEXT.md
@.planning/phases/07-production-persistence-and-ci/07-RESEARCH.md
@.planning/phases/07-production-persistence-and-ci/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: SQL init scripts and app.py lifespan -->

db/migrations/001_init.sql -- creates runs, graph_checkpoints, memo_entries tables
db/migrations/002_foundation.sql -- creates pgvector extension + v2 foundation tables

src/agentic_workflows/api/app.py lifespan:
- Reads DATABASE_URL from env
- If set: creates psycopg_pool.ConnectionPool + Postgres stores
- If absent: creates SQLite stores
- Runs uvicorn on 0.0.0.0:8000

Existing .github/workflows/ci.yml:
- Single job "quality" with lint, typecheck, test steps
- P1_PROVIDER=scripted in env
- No matrix, no Postgres, no coverage
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile, docker-compose.yml, .dockerignore, and Makefile Docker targets</name>
  <files>
    Dockerfile
    docker-compose.yml
    .dockerignore
    Makefile
  </files>
  <action>
    **1. Dockerfile** (single-stage per user decision):
    - FROM python:3.12-slim
    - WORKDIR /app
    - psycopg[binary] bundles libpq so no system deps needed
    - COPY pyproject.toml first, then COPY src/ src/, then RUN pip install --no-cache-dir .
    - Do NOT install [dev] deps in production image
    - Do NOT use pip install -e (editable mode) in Docker -- use pip install .
    - EXPOSE 8000
    - CMD uvicorn agentic_workflows.api.app:app --host 0.0.0.0 --port 8000

    **2. docker-compose.yml**:
    - postgres service: image pgvector/pgvector:pg16, env POSTGRES_USER/PASSWORD/DB=agentic/agentic/agentic_workflows, port 5432:5432, named volume pgdata:/var/lib/postgresql/data, mount ./db/migrations:/docker-entrypoint-initdb.d, healthcheck with pg_isready -U agentic (interval 5s, timeout 5s, retries 5)
    - api service: build ., port 8000:8000, env DATABASE_URL=postgresql://agentic:agentic@postgres:5432/agentic_workflows and P1_PROVIDER=${P1_PROVIDER:-openai}, depends_on postgres condition service_healthy, env_file .env (required: false)
    - volumes: pgdata (named volume for persistence)
    - Add comment noting docker compose down -v is needed to re-run init scripts after schema changes (Pitfall 5 from RESEARCH.md)

    **3. .dockerignore**: Exclude .venv/, .git/, .tmp/, __pycache__/, *.pyc, .env, .planning/, docs/, tests/, .github/, .ruff_cache/, *.egg-info/, dist/, .mypy_cache/

    **4. Makefile additions** (append to existing):
    - docker-build: docker build -t agentic-workflows .
    - docker-up: docker compose up -d
    - docker-down: docker compose down
    - docker-reset: docker compose down -v (wipes volume for schema changes)
    - docker-logs: docker compose logs -f
    - Add these to .PHONY line
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && docker build -t agentic-workflows-test . 2>&1 | tail -5 && echo "--- Docker build succeeded ---"</automated>
  </verify>
  <done>
    - Dockerfile builds successfully producing a runnable image
    - docker-compose.yml defines postgres and api services with health checks
    - .dockerignore excludes non-production files from Docker context
    - Makefile has docker-build, docker-up, docker-down, docker-reset, docker-logs targets
    - Named volume pgdata defined for Postgres persistence
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend CI workflow with Postgres service, test matrix, Docker build, and coverage</name>
  <files>
    .github/workflows/ci.yml
    pyproject.toml
  </files>
  <action>
    **1. Rewrite .github/workflows/ci.yml** with two jobs:

    **Job 1: quality** (existing, now with matrix):
    - strategy.matrix.backend: [sqlite, postgres]
    - services block with Postgres container: image pgvector/pgvector:pg16, env POSTGRES_USER=test POSTGRES_PASSWORD=test POSTGRES_DB=test_agentic, ports 5432:5432, options --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
    - Steps: checkout, setup-python 3.12, pip install -e ".[dev]"
    - Lint (ruff check): only on sqlite leg (if: matrix.backend == 'sqlite') to avoid duplicate
    - Typecheck (mypy): only on sqlite leg
    - Init Postgres step (if: matrix.backend == 'postgres'): install postgresql-client, then run psql -h localhost -U test -d test_agentic -f db/migrations/001_init.sql and 002_foundation.sql with PGPASSWORD=test (CI services don't mount repo volumes)
    - Test SQLite step (if: matrix.backend == 'sqlite'): pytest tests/ -q --cov=src/agentic_workflows --cov-report=term-missing --cov-fail-under=80, env P1_PROVIDER=scripted
    - Test Postgres step (if: matrix.backend == 'postgres'): same pytest command, env P1_PROVIDER=scripted and DATABASE_URL=postgresql://test:test@localhost:5432/test_agentic

    **Job 2: docker-build** (new):
    - name: Docker Build Test
    - runs-on: ubuntu-latest
    - Steps: checkout, docker build -t agentic-workflows-test .
    - No push, build-only verification

    **2. pyproject.toml updates**:
    - Add markers list to [tool.pytest.ini_options]: markers = ["postgres = requires PostgreSQL database (DATABASE_URL env var)"]
    - Do NOT add --cov to default addopts (would slow local dev). Coverage enforced only in CI.

    **IMPORTANT per RESEARCH.md:**
    - Postgres service container needs pg_isready health check options
    - Init scripts run manually via psql in CI (no volume mount)
    - ScriptedProvider only -- no live LLM credentials in CI
    - 80% coverage threshold per user decision
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && python3 -c "
content = open('.github/workflows/ci.yml').read()
checks = [
    ('matrix' in content, 'matrix strategy'),
    ('docker' in content.lower(), 'docker build job'),
    ('cov-fail-under' in content, 'coverage threshold'),
    ('pgvector' in content, 'postgres service'),
    ('scripted' in content, 'ScriptedProvider'),
    ('001_init.sql' in content, 'init script execution'),
]
for ok, name in checks:
    status = 'PASS' if ok else 'FAIL'
    print(f'  {status}: {name}')
    assert ok, f'{name} missing from ci.yml'
print('All CI workflow checks passed')
"</automated>
  </verify>
  <done>
    - CI workflow has quality job with sqlite/postgres matrix
    - CI workflow has docker-build job
    - Postgres service container configured with health checks
    - Init scripts executed via psql in Postgres matrix leg
    - pytest-cov with 80% threshold in both matrix legs
    - postgres marker registered in pyproject.toml
    - No live LLM credentials in any CI step
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Docker and CI setup</name>
  <action>Human verification of Docker containerization and CI pipeline.</action>
  <what-built>
    Docker containerization of the full system (Postgres + FastAPI) and CI pipeline with Postgres service, test matrix, Docker build test, and coverage enforcement.
  </what-built>
  <how-to-verify>
    1. Run `docker compose up -d` from project root
    2. Wait for both services to be healthy: `docker compose ps` (both should show "healthy" or "Up")
    3. Test the API: `curl http://localhost:8000/health` -- should return 200
    4. Verify Postgres is used: check docker compose logs for "storage=postgres" at startup
    5. Stop and restart: `docker compose restart` -- verify /health still works (data persistence)
    6. Clean up: `docker compose down -v`
    7. Review .github/workflows/ci.yml for correctness (matrix, services, coverage)
  </how-to-verify>
  <done>User has verified Docker and CI setup work correctly.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Docker build succeeds: `docker build -t agentic-workflows-test .`
- docker-compose.yml is valid: `docker compose config`
- CI workflow YAML is valid (no syntax errors)
- All existing tests still pass
- Makefile Docker targets work
</verification>

<success_criteria>
- Dockerfile produces a runnable image with the FastAPI service
- docker-compose up starts Postgres + API, API connects to Postgres successfully
- Postgres data persists across container restarts (named volume)
- CI workflow has sqlite/postgres test matrix with coverage enforcement
- CI workflow has Docker build test job
- No live LLM credentials anywhere in CI
- 80% coverage threshold enforced via pytest-cov
</success_criteria>

<output>
After completion, create `.planning/phases/07-production-persistence-and-ci/07-03-SUMMARY.md`
</output>
