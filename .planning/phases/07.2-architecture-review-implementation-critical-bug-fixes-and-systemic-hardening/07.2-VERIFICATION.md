---
phase: 07.2-architecture-review-implementation-critical-bug-fixes-and-systemic-hardening
verified: 2026-03-08T03:30:00Z
status: passed
score: 12/12 must-haves verified
---

# Phase 07.2: Architecture Review Implementation Verification Report

**Phase Goal:** Fix 2 correctness bugs (dual tool execution, _active_callbacks data race), 5 critical bottlenecks (SQLite WAL, seen_tool_signatures set conversion, pipeline_trace/handoff caps), and 3 structural/safety improvements (_ANNOTATED_LIST_FIELDS auto-derivation, prepare_state() extraction, run_bash guard, memoize prompt removal, tool contract tests). All changes are internal -- no observable behavior change.
**Verified:** 2026-03-08T03:30:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A tool called via _route_to_specialist executes exactly once -- no duplicate side effects | VERIFIED | `_executor_subgraph.invoke` does not appear as an active call in `_route_to_specialist` (graph.py:1478-1485); only appears in a comment explaining the fix. `_execute_action()` is the sole execution path. |
| 2 | Two concurrent run() calls each see their own _active_callbacks list via ContextVar | VERIFIED | Module-level `_active_callbacks_var: contextvars.ContextVar[list]` at graph.py:117-119. `self._active_callbacks` instance field fully removed (`grep -r "self._active_callbacks" src/` returns 0 results). Thread isolation test exists and passes in test_run_helpers.py. |
| 3 | SSE streaming path scopes callbacks per-request via ContextVar | VERIFIED | routes/run.py:93 imports `_active_callbacks_var`, line 95 calls `.set()`, line 99 calls `.get()`. No instance field mutation. |
| 4 | SQLiteCheckpointStore uses one persistent connection with WAL mode | VERIFIED | checkpoint_store.py:48-57 -- `__init__` creates `self._conn`, `self._lock`, executes `PRAGMA journal_mode=WAL` and `PRAGMA busy_timeout=5000`. `_connect()` method fully removed. All methods use `with self._lock: self._conn.execute(...)`. |
| 5 | seen_tool_signatures is a plain set[str] with O(1) membership checks | VERIFIED | state_schema.py:85 -- `seen_tool_signatures: set[str]`. new_run_state() initializes as `set()` (line 156). ensure_state_defaults() initializes as `set()` and converts `list->set` if loaded from checkpoint (lines 204-207). graph.py:1760 uses `.add()`. _dedup_then_tool_node uses `set()` default (line 339). |
| 6 | pipeline_trace capped at 500 entries; handoff_queue and handoff_results capped at 50 | VERIFIED | graph.py:109-111 defines `_PIPELINE_TRACE_CAP = 500`, `_HANDOFF_QUEUE_CAP = 50`, `_HANDOFF_RESULTS_CAP = 50`. Eviction in `_emit_trace` (graph.py:527-528), handoff_queue (graph.py:1475-1476), handoff_results (graph.py:1515-1516). context_manager.py also has local `_PIPELINE_TRACE_CAP = 500` with eviction (lines 503-504). |
| 7 | State with seen_tool_signatures survives checkpoint round-trip as set | VERIFIED | checkpoint_store.py:34-38 -- `_json_default` serializes sets as `sorted(x)`. state_schema.py:206-207 -- `ensure_state_defaults` converts `list->set` on deserialization. Test `test_seen_tool_signatures_checkpoint_roundtrip` in test_state_schema.py passes. |
| 8 | _ANNOTATED_LIST_FIELDS is auto-derived at import time from RunState annotations | VERIFIED | graph.py:94-106 -- `_derive_annotated_list_fields()` uses `typing.get_type_hints(RunState, include_extras=True)` and introspects for `Annotated[list, operator.add]`. `_ANNOTATED_LIST_FIELDS` assigned from function call, not a literal frozenset. |
| 9 | prepare_state() is the single source of truth for state initialization | VERIFIED | graph.py:421-475 -- `prepare_state()` public method encapsulates new_run_state + prior_context merge + ensure_state_defaults + mission parsing + checkpoint save. `run()` calls `self.prepare_state()` (graph.py:490). SSE path calls `orchestrator.prepare_state()` (routes/run.py:85-88). |
| 10 | run_bash returns error dict when P1_BASH_ENABLED is not set | VERIFIED | run_bash.py:35-37 -- `if os.environ.get("P1_BASH_ENABLED", "").strip().lower() != "true": return {"error": "run_bash is disabled; set P1_BASH_ENABLED=true to enable"}`. Test `test_run_bash_disabled_by_default` passes. |
| 11 | Planner system prompt does not contain "- memoize:" in tool list | VERIFIED | `grep "- memoize:" graph.py` returns 0 matches. graph.py:252-256 shows updated memoization policy: "Memoization is automatic". `retrieve_memo` still present (line 232). |
| 12 | All 35+ registered tools pass name, description, callable execute, and returns-dict contracts | VERIFIED | test_tool_contracts.py: 144 parametrized tests (4 contracts x 36 tools) all pass. No stubs remain -- `grep "pytest.fail" test_tool_contracts.py` returns 0 results. |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/agentic_workflows/orchestration/langgraph/graph.py` | ContextVar, auto-derive, prepare_state, caps, dual-exec removal, memoize removal | VERIFIED | All patterns present and wired; 1737+ lines |
| `src/agentic_workflows/orchestration/langgraph/checkpoint_store.py` | Persistent conn + WAL + set serializer | VERIFIED | 141 lines; `_conn`, `_lock`, WAL pragma, `_json_default` with set handler |
| `src/agentic_workflows/orchestration/langgraph/state_schema.py` | seen_tool_signatures: set[str] | VERIFIED | Line 85; new_run_state and ensure_state_defaults both use set() |
| `src/agentic_workflows/orchestration/langgraph/context_manager.py` | pipeline_trace cap | VERIFIED | `_PIPELINE_TRACE_CAP = 500` at line 24; eviction at lines 503-504 |
| `src/agentic_workflows/api/routes/run.py` | ContextVar import + prepare_state call | VERIFIED | Lines 85-88 (prepare_state), lines 93-95 (_active_callbacks_var.set) |
| `src/agentic_workflows/tools/run_bash.py` | P1_BASH_ENABLED guard | VERIFIED | Lines 35-37; early return before any execution |
| `tests/unit/test_tool_contracts.py` | 144 passing parametrized tests | VERIFIED | 57 lines; 144 tests pass; no stubs |
| `tests/unit/test_subgraph_routing.py` | No-invoke assertion test | VERIFIED | Test passes (52 tests total across phase test files) |
| `tests/unit/test_run_helpers.py` | ContextVar, caps, prepare_state, memoize tests | VERIFIED | Tests pass (part of 52 test count) |
| `tests/unit/test_state_schema.py` | Set type, roundtrip, auto-derivation tests | VERIFIED | Tests pass |
| `tests/unit/test_run_bash.py` | Disabled-by-default test | VERIFIED | 6 tests pass |
| `tests/unit/test_tool_security.py` | Updated for P1_BASH_ENABLED | VERIFIED | Tests pass with monkeypatch.setenv |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| routes/run.py | graph._active_callbacks_var | import + .set() | WIRED | Line 93: import; line 95: `.set()`; line 99: `.get()` |
| graph.run() | _active_callbacks_var | .set() call | WIRED | Line 489: `.set()` |
| _derive_annotated_list_fields | RunState.__annotations__ | typing.get_type_hints | WIRED | Line 96: `typing.get_type_hints(RunState, include_extras=True)` |
| routes/run._run_streaming | orchestrator.prepare_state | direct method call | WIRED | Lines 85-88: `orchestrator.prepare_state(user_input, run_id=run_id, prior_context=prior_context)` |
| graph.run() | self.prepare_state | direct method call | WIRED | Line 490-495: `state = self.prepare_state(...)` |
| test_tool_contracts.py | build_tool_registry | module-level fixture | WIRED | Lines 13-22: imports and calls `build_tool_registry` |
| run_bash.execute() | os.environ.get('P1_BASH_ENABLED') | early return guard | WIRED | Lines 35-37: guard at top of execute() |
| ensure_state_defaults | seen_tool_signatures set conversion | list-to-set | WIRED | Lines 204-207: `set()` init + `set(state_dict["seen_tool_signatures"])` conversion |
| _execute_action | seen_tool_signatures.add() | .add() not .append() | WIRED | Line 1760: `state["seen_tool_signatures"].add(signature)` |

### Requirements Coverage

No external requirement IDs for this phase (internal fixes only). All plans have `requirements: []` in frontmatter.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| graph.py | 118 | B039: Mutable default `[]` for ContextVar | Warning | Ruff lint warning; functionally correct since ContextVar copies defaults per-context. Not a blocker. |
| test_run_helpers.py | 185,191,200,232,257 | I001: Import block unsorted | Warning | 5 import ordering issues in test file; cosmetic. |
| test_run_helpers.py | 305 | B009: getattr with constant attribute | Warning | Cosmetic; functionally equivalent. |
| test_state_schema.py | 152 | SIM300: Yoda condition | Warning | Cosmetic; assertion works correctly. |
| test_context_manager.py | 3,5 | I001+F401: Unsorted imports + unused pytest | Warning | Pre-existing pattern; `pytest` imported but unused. |

**Note:** Main branch has 3 pre-existing ruff errors. This phase introduced 7 new lint warnings (all in test files or cosmetic). None are blockers -- all 823 tests pass.

### Human Verification Required

### 1. Concurrent SSE Callback Isolation

**Test:** Start two simultaneous SSE runs via POST /run with different prompts and verify Langfuse traces are scoped to each run independently.
**Expected:** Each run's Langfuse spans contain only events from that specific run, not cross-contaminated.
**Why human:** ContextVar isolation is verified in unit tests for threads, but actual SSE + anyio.to_thread integration requires a live server and real Langfuse observation.

### 2. run_bash Behavior in Production

**Test:** Run the orchestrator with a mission requiring bash execution. Verify it fails gracefully when P1_BASH_ENABLED is not set, and succeeds when set to "true".
**Expected:** Without env var: run proceeds but bash tool calls return error dicts that the planner handles. With env var: commands execute normally.
**Why human:** Integration behavior between the planner's reaction to the error dict and mission completion cannot be fully verified without a live provider.

### Gaps Summary

No gaps found. All 12 observable truths verified against the codebase. All artifacts exist, are substantive (no stubs), and are properly wired. Key links confirmed via grep. 823 tests pass. 7 new cosmetic ruff lint warnings are not blockers.

---

_Verified: 2026-03-08T03:30:00Z_
_Verifier: Claude (gsd-verifier)_
