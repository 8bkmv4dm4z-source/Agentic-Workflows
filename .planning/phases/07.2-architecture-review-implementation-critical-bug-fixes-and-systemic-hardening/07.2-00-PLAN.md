---
phase: 07.2-architecture-review-implementation-critical-bug-fixes-and-systemic-hardening
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_tool_contracts.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "tests/unit/test_tool_contracts.py exists with parametrized stubs that fail with a clear MISSING marker"
    - "pytest can collect the file without errors"
  artifacts:
    - path: "tests/unit/test_tool_contracts.py"
      provides: "Parametrized test stubs for all 35+ registered tools"
      min_lines: 40
  key_links:
    - from: "tests/unit/test_tool_contracts.py"
      to: "agentic_workflows.orchestration.langgraph.tools_registry.build_tool_registry"
      via: "import and fixture"
      pattern: "build_tool_registry"
---

<objective>
Create the test scaffold for the parametrized tool contract tests (W3-10). The file must be importable and collectable by pytest before any implementation exists. Stubs are marked with `pytest.fail("MISSING — Wave 3 plan 04 must implement")` so that Wave 3 tasks have a clear failing target.

Purpose: Satisfy the Nyquist rule — every wave-3 task that produces code must have a failing test waiting for it. This plan creates that failing baseline.
Output: tests/unit/test_tool_contracts.py with stub parametrized tests that fail at collection time.
</objective>

<execution_context>
@/home/nir/.claude/get-shit-done/workflows/execute-plan.md
@/home/nir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07.2-architecture-review-implementation-critical-bug-fixes-and-systemic-hardening/07.2-CONTEXT.md
@.planning/phases/07.2-architecture-review-implementation-critical-bug-fixes-and-systemic-hardening/07.2-RESEARCH.md
@.planning/phases/07.2-architecture-review-implementation-critical-bug-fixes-and-systemic-hardening/07.2-VALIDATION.md

<interfaces>
<!-- Key types and imports the executor needs. -->

From src/agentic_workflows/orchestration/langgraph/tools_registry.py:
```python
# build_tool_registry(memo_store, *, checkpoint_store=None) -> dict[str, BaseTool]
# Returns a dict mapping tool name -> BaseTool instance
from agentic_workflows.orchestration.langgraph.tools_registry import build_tool_registry
```

From src/agentic_workflows/orchestration/langgraph/memo_store.py:
```python
from agentic_workflows.orchestration.langgraph.memo_store import SQLiteMemoStore
```

From src/agentic_workflows/orchestration/langgraph/checkpoint_store.py:
```python
from agentic_workflows.orchestration.langgraph.checkpoint_store import SQLiteCheckpointStore
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_tool_contracts.py with MISSING stubs</name>
  <files>tests/unit/test_tool_contracts.py</files>
  <action>
Create tests/unit/test_tool_contracts.py with the following structure:

1. A module-level `_build_all_tools()` helper that instantiates the tool registry using in-memory stores:
   ```python
   from agentic_workflows.orchestration.langgraph.tools_registry import build_tool_registry
   from agentic_workflows.orchestration.langgraph.memo_store import SQLiteMemoStore
   from agentic_workflows.orchestration.langgraph.checkpoint_store import SQLiteCheckpointStore

   def _build_all_tools() -> dict:
       memo = SQLiteMemoStore(":memory:")
       ck = SQLiteCheckpointStore(".tmp/test_contracts.db")
       return build_tool_registry(memo, checkpoint_store=ck)

   _ALL_TOOLS = list(_build_all_tools().items())  # [(name, tool), ...]
   ```

2. Four parametrized test stubs, each marked `pytest.fail("MISSING — implement in plan 07.2-04")`:
   ```python
   import pytest

   @pytest.mark.parametrize("tool_name,tool", _ALL_TOOLS, ids=[t[0] for t in _ALL_TOOLS])
   def test_tool_has_name(tool_name, tool):
       pytest.fail("MISSING — implement in plan 07.2-04")

   @pytest.mark.parametrize("tool_name,tool", _ALL_TOOLS, ids=[t[0] for t in _ALL_TOOLS])
   def test_tool_has_description(tool_name, tool):
       pytest.fail("MISSING — implement in plan 07.2-04")

   @pytest.mark.parametrize("tool_name,tool", _ALL_TOOLS, ids=[t[0] for t in _ALL_TOOLS])
   def test_tool_execute_is_callable(tool_name, tool):
       pytest.fail("MISSING — implement in plan 07.2-04")

   @pytest.mark.parametrize("tool_name,tool", _ALL_TOOLS, ids=[t[0] for t in _ALL_TOOLS])
   def test_tool_execute_returns_dict_on_empty_args(tool_name, tool):
       pytest.fail("MISSING — implement in plan 07.2-04")
   ```

3. Add a docstring at the top:
   ```python
   """Parametrized contract tests for all registered tools (W3-10).

   Verifies each tool has: name (str), description (str), callable execute(), execute({}) returns dict.
   Stubs replaced with assertions in plan 07.2-04.
   """
   ```

Do NOT import from graph.py or state_schema.py — only tools_registry, memo_store, checkpoint_store.
</action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && pytest tests/unit/test_tool_contracts.py --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <done>pytest can collect tests/unit/test_tool_contracts.py without import errors; collected test count is 4 × (number of tools) ≥ 140 parametrized cases; all collected tests are listed as FAILED (not ERROR) when run</done>
</task>

</tasks>

<verification>
pytest tests/unit/test_tool_contracts.py --collect-only -q — must collect without errors and show 4 × N parametrized tests.
</verification>

<success_criteria>
tests/unit/test_tool_contracts.py exists, is importable, and pytest --collect-only succeeds. The file serves as the failing test baseline for W3-10 in plan 07.2-04.
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-architecture-review-implementation-critical-bug-fixes-and-systemic-hardening/07.2-00-SUMMARY.md`
</output>
