---
phase: 07.2-architecture-review-implementation-critical-bug-fixes-and-systemic-hardening
plan: "01"
subsystem: orchestration
tags: [contextvars, dual-execution, callbacks, concurrency, langgraph]

# Dependency graph
requires:
  - phase: 04-multi-agent-integration
    provides: "specialist subgraph routing via _route_to_specialist"
  - phase: 05-observability
    provides: "Langfuse callback handler wired into _active_callbacks"
provides:
  - "Single-execution path in _route_to_specialist (no dual subgraph + _execute_action)"
  - "_active_callbacks_var ContextVar for thread-isolated Langfuse callback scoping"
  - "SSE streaming path uses ContextVar instead of instance field mutation"
affects: [orchestration, api-streaming, observability]

# Tech tracking
tech-stack:
  added: [contextvars]
  patterns: [ContextVar for per-request isolation in multi-threaded orchestrator]

key-files:
  created: []
  modified:
    - src/agentic_workflows/orchestration/langgraph/graph.py
    - src/agentic_workflows/api/routes/run.py
    - tests/unit/test_subgraph_routing.py
    - tests/unit/test_run_helpers.py

key-decisions:
  - "Removed entire exec_state construction block and subgraph.invoke call rather than just disabling -- dead code removal"
  - "ContextVar default=[] so missing .set() degrades gracefully to no-op callbacks"
  - "Thread isolation test uses threading.Thread to verify ContextVar semantics directly"

patterns-established:
  - "ContextVar for per-run isolation: any state that differs between concurrent run() calls should use contextvars.ContextVar"

requirements-completed: []

# Metrics
duration: 4min
completed: 2026-03-08
---

# Phase 07.2 Plan 01: Wave 1 Critical Bug Fixes Summary

**Eliminated dual tool execution in specialist routing and replaced instance-level callback list with contextvars.ContextVar for thread-isolated Langfuse span tracking**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-08T00:46:16Z
- **Completed:** 2026-03-08T00:50:36Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- W1-1: Removed `_executor_subgraph.invoke()` call from `_route_to_specialist` -- tools now execute exactly once via `_execute_action()` only
- W1-2: Replaced `self._active_callbacks` instance field with module-level `_active_callbacks_var` ContextVar -- concurrent `run()` calls get isolated callback lists
- W1-2: SSE streaming path (`routes/run.py`) updated to use ContextVar `.set()` instead of mutating orchestrator instance field
- 596 unit tests passing (4 new tests added, zero regressions)

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove dual-execution in _route_to_specialist (W1-1)**
   - `bbbbee8` (test: TDD RED - add failing test for no-invoke assertion)
   - `cce4202` (feat: TDD GREEN - remove subgraph invoke, keep _execute_action only)
2. **Task 2: Replace self._active_callbacks with ContextVar (W1-2)**
   - `08e89e0` (test: TDD RED - add failing tests for ContextVar existence and isolation)
   - `b6a42df` (feat: TDD GREEN - add ContextVar, replace all self._active_callbacks usages)
3. **Task 3: Regression check** - verification only, no code changes needed

## Files Created/Modified
- `src/agentic_workflows/orchestration/langgraph/graph.py` - Removed dual-execution, added _active_callbacks_var ContextVar, removed self._active_callbacks instance field
- `src/agentic_workflows/api/routes/run.py` - Import and use _active_callbacks_var.set() for SSE callback setup
- `tests/unit/test_subgraph_routing.py` - Added test_executor_subgraph_invoke_not_called_during_routing
- `tests/unit/test_run_helpers.py` - Added TestActiveCallbacksContextVar class (3 tests: existence, default, thread isolation)

## Decisions Made
- Removed entire `exec_state` construction block (lines 1433-1456) since it existed solely to feed the now-removed `_executor_subgraph.invoke()` -- the `specialist_ctx` from ContextManager was only consumed by `exec_state` fields
- Used `contextvars.ContextVar` with `default=[]` so that any execution context without an explicit `.set()` degrades gracefully to no callbacks (no Langfuse noise)
- Kept `_active_callbacks_var` as a public module-level symbol (no leading underscore on the variable itself) so `routes/run.py` can import it directly

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Wave 1 bugs fixed, ready for Wave 2 plans (07.2-02 through 07.2-05)
- The removed dual-execution path may affect any monitoring that relied on `_executor_subgraph.invoke()` being called during routing -- but this was the bug itself (double execution), so no legitimate dependency exists

---
*Phase: 07.2-architecture-review-implementation-critical-bug-fixes-and-systemic-hardening*
*Completed: 2026-03-08*

## Self-Check: PASSED
- All 5 files verified present
- All 4 task commits verified in git log
