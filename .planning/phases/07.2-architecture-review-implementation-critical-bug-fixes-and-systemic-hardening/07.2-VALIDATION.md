---
phase: 07.2
slug: architecture-review-implementation-critical-bug-fixes-and-systemic-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-08
---

# Phase 07.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (already installed in `.[dev]`) |
| **Config file** | `pyproject.toml` (pytest section, `asyncio_mode = auto`) |
| **Quick run command** | `pytest tests/unit/ -q` |
| **Full suite command** | `pytest tests/ -q` |
| **Estimated runtime** | ~30 seconds (unit), ~90 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/unit/ -q`
- **After every plan wave:** Run `pytest tests/ -q`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds (unit), 90 seconds (full)

---

## Per-Task Verification Map

| Task ID | Fix | Wave | Test Type | Automated Command | File Exists | Status |
|---------|-----|------|-----------|-------------------|-------------|--------|
| W1-1 | Dual-execution removed — tool fires exactly once | 1 | unit | `pytest tests/unit/test_subgraph_routing.py -x -q` | ✅ extend | ⬜ pending |
| W1-2 | `_active_callbacks` scoped per-run via ContextVar | 1 | unit | `pytest tests/unit/test_run_helpers.py -x -q` | ✅ extend | ⬜ pending |
| W2-3 | SQLiteCheckpointStore uses WAL + persistent conn | 2 | unit | `pytest tests/unit/test_state_schema.py -x -q` | ✅ extend | ⬜ pending |
| W2-4 | `seen_tool_signatures` is a plain set (O(1) lookup) | 2 | unit | `pytest tests/unit/test_state_schema.py -x -q` | ✅ extend | ⬜ pending |
| W2-5 | `pipeline_trace`/`handoff_queue`/`handoff_results` capped | 2 | unit | `pytest tests/unit/test_run_helpers.py -x -q` | ✅ extend | ⬜ pending |
| W3-6 | `_ANNOTATED_LIST_FIELDS` auto-derived from annotations | 3 | unit | `pytest tests/unit/test_state_schema.py::test_annotated_list_fields_synchronized -x` | ✅ | ⬜ pending |
| W3-7 | `prepare_state()` extracted; run() and SSE use it | 3 | unit | `pytest tests/unit/test_run_helpers.py -x -q` | ✅ extend | ⬜ pending |
| W3-8 | `run_bash` disabled by default without P1_BASH_ENABLED | 3 | unit | `pytest tests/unit/test_run_bash.py -x -q` | ✅ extend | ⬜ pending |
| W3-9 | `memoize` absent from planner tool list in prompt | 3 | unit | `pytest tests/unit/test_run_helpers.py -x -q` | ✅ extend | ⬜ pending |
| W3-10 | Parametrized contract test for all 35+ tools | 0 | unit | `pytest tests/unit/test_tool_contracts.py -x -q` | ❌ Wave 0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/test_tool_contracts.py` — parametrized contract test for all 35+ registered tools (W3-10)

All other fixes extend existing test files:
- `tests/unit/test_subgraph_routing.py` — add W1-1 assertion that `_executor_subgraph.invoke` is not called during normal execution
- `tests/unit/test_run_helpers.py` — add W1-2, W2-5, W3-7, W3-9 assertions
- `tests/unit/test_state_schema.py` — update W2-4 (seen_tool_signatures is now set); add W2-3 checkpoint store WAL test
- `tests/unit/test_run_bash.py` — add W3-8 disabled-by-default test; update existing tests to set `P1_BASH_ENABLED=true`

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| Concurrent run() calls don't clobber Langfuse spans | Requires live Langfuse instance | Set `LANGFUSE_*` vars, run two concurrent `run()` calls, verify each span has correct callbacks |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (test_tool_contracts.py)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (unit), < 90s (full)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
