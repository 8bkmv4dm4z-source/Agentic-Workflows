---
phase: 02-langgraph-upgrade-and-single-agent-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/agentic_workflows/orchestration/langgraph/run.py
  - docs/ADR/ADR-001-langgraph-version-upgrade.md
  - docs/ADR/ADR-002-toolnode-anthropic-path.md
  - docs/ADR/ADR-003-annotated-reducers.md
  - docs/ADR/ADR-004-message-compaction.md
autonomous: true
requirements:
  - OBSV-02
  - LRNG-02

must_haves:
  truths:
    - "@observe() decorator is applied to run() in run.py"
    - "docs/ADR/ directory exists with at least one ADR"
    - "ADR-001 documents the langgraph<1.0 pin removal with Status, Context, Decision, Consequences sections"
    - "The @observe() addition causes no test failures (graceful degradation when LANGFUSE_PUBLIC_KEY is absent)"
  artifacts:
    - path: "src/agentic_workflows/orchestration/langgraph/run.py"
      provides: "@observe() wired on run() entrypoint"
      contains: "@observe"
    - path: "docs/ADR/ADR-001-langgraph-version-upgrade.md"
      provides: "Decision record for pin removal"
      contains: "## Context"
    - path: "docs/ADR/ADR-002-toolnode-anthropic-path.md"
      provides: "Decision record for ToolNode scope"
      contains: "## Decision"
    - path: "docs/ADR/ADR-003-annotated-reducers.md"
      provides: "Decision record for Annotated reducers"
      contains: "## Consequences"
    - path: "docs/ADR/ADR-004-message-compaction.md"
      provides: "Decision record for message compaction strategy"
      contains: "## Consequences"
  key_links:
    - from: "run.py run()"
      to: "observability.py observe()"
      via: "@observe(name='run') decorator"
      pattern: "@observe"
---

<objective>
Wire the existing `@observe()` decorator onto `run()` in `run.py` (the open Phase 1 item), and establish the `docs/ADR/` directory with four ADRs documenting the key Phase 2 architectural decisions.

Purpose: `@observe()` closes the open OBSV-02 item from Phase 1. The ADR log (LRNG-02) makes architectural decisions explicit and reviewable as part of the learning-driven build approach. Both are small independent changes that can run parallel with the reducer work (Plan 02).

Output: `run.py` with `@observe()` on `run()`; `docs/ADR/` directory with 4 ADRs.
</objective>

<execution_context>
@/home/nir/.claude/get-shit-done/workflows/execute-plan.md
@/home/nir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-langgraph-upgrade-and-single-agent-hardening/02-CONTEXT.md
@.planning/phases/02-langgraph-upgrade-and-single-agent-hardening/02-RESEARCH.md
@.planning/phases/02-langgraph-upgrade-and-single-agent-hardening/02-01-SUMMARY.md
@src/agentic_workflows/observability.py
</context>

<interfaces>
<!-- observability.py — already correct, just apply the decorator -->
<!-- From src/agentic_workflows/observability.py (inspected): -->
<!-- @observe() is a graceful-degradation stub: -->
<!--   - If langfuse not installed: returns passthrough decorator -->
<!--   - If LANGFUSE_PUBLIC_KEY not set: returns passthrough decorator -->
<!--   - If configured: wraps with Langfuse tracing -->

<!-- provider.py already has @observe() on OpenAI, Groq, Ollama generate() methods: -->
<!--   line 137: @observe("provider.generate.openai") -->
<!--   line 165: @observe("provider.generate.groq") -->
<!--   line 196: @observe("provider.generate.ollama") -->
<!-- run.py run() is the ONLY remaining undecorated target. -->

<!-- ADR format (from CONTEXT.md): -->
<!-- Sections: Status, Context, Decision, Consequences -->
<!-- Location: docs/ADR/ directory -->
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire @observe() on run() in run.py</name>
  <files>
    src/agentic_workflows/orchestration/langgraph/run.py
  </files>
  <action>
Read `src/agentic_workflows/orchestration/langgraph/run.py` to find:
1. The `run()` function definition (it is the main orchestration entrypoint used in tests and CLI)
2. Whether `observe` is already imported (check existing imports at top of file)

Then make these changes:

1. If `observe` is not already imported, add this import near the other agentic_workflows imports:
```python
from agentic_workflows.observability import observe
```

2. Add the decorator on the `run()` function:
```python
@observe(name="run")
def run(mission: str, ...) -> dict:
    ...
```

Use `@observe(name="run")` (with the `name=` keyword argument) to produce clean span names in Langfuse traces. Match the exact decorator pattern used in `provider.py` for consistency.

IMPORTANT: The `@observe()` decorator has graceful degradation built in. When `LANGFUSE_PUBLIC_KEY` is not set (CI, dev without Langfuse), it returns a passthrough decorator. No env vars are required. No tests need to change.

After editing, run:
```
cd /home/nir/dev/agent_phase0 && pytest tests/ -q 2>&1 | tail -5
```
All tests must still pass.
  </action>
  <verify>
    <automated>grep -n "@observe" /home/nir/dev/agent_phase0/src/agentic_workflows/orchestration/langgraph/run.py | head -5</automated>
  </verify>
  <done>`@observe` decorator is present in run.py. All tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create docs/ADR/ directory with four ADRs</name>
  <files>
    docs/ADR/ADR-001-langgraph-version-upgrade.md
    docs/ADR/ADR-002-toolnode-anthropic-path.md
    docs/ADR/ADR-003-annotated-reducers.md
    docs/ADR/ADR-004-message-compaction.md
  </files>
  <action>
Create the `docs/ADR/` directory and four ADR files. Each file uses the four-section format: **Status**, **Context**, **Decision**, **Consequences**.

**ADR-001-langgraph-version-upgrade.md:**
```markdown
# ADR-001: Remove langgraph<1.0 Version Pin

**Status:** Accepted
**Date:** 2026-03-02

## Context
The `langgraph<1.0` pin was added during Phase 1 to maintain stability while the
LangGraph 1.0 API surface stabilized. Phase 2 requires `ToolNode`, `tools_condition`,
and `Annotated` reducer annotations — all of which are only available in langgraph>=1.0.
The pin became the master blocker for the entire roadmap.

## Decision
Remove the `langgraph<1.0` pin. Pin to `langgraph>=1.0.6,<2.0` and
`langgraph-prebuilt>=1.0.1,<1.0.2` (stable combination: prebuilt 1.0.2 broke the
`ToolNode.afunc` signature — GitHub Issue #6363). Add `langchain-anthropic>=0.3.0`
as a new dependency required for the Anthropic ToolNode path.

## Consequences
- ToolNode and tools_condition become available from langgraph-prebuilt
- Annotated reducer syntax is supported for RunState list fields
- langgraph-prebuilt is pinned to 1.0.1 (not 1.0.2+) until the afunc fix is confirmed in a higher version
- All 208 existing tests must pass unchanged — LangGraph 1.0 is backwards-compatible with 0.2.x APIs used in this codebase
- Upgrade is applied in isolation (no behavioral changes in the same commit)
```

**ADR-002-toolnode-anthropic-path.md:**
```markdown
# ADR-002: ToolNode Adoption — Anthropic Path Only

**Status:** Accepted
**Date:** 2026-03-02

## Context
The manual XML/JSON envelope parser in `graph.py` (`_execute_tool()`, `_handle_tool_calls()`)
is the most fragile part of the Phase 1 codebase. LangGraph 1.0 provides `ToolNode` from
`langgraph-prebuilt` as a standard, well-tested replacement that handles format differences
across providers. However, migrating all three provider paths at once increases risk of
regressions and testing complexity.

## Decision
Adopt `ToolNode` + `tools_condition` for the Anthropic provider path only in Phase 2.
Ollama (primary dev provider), OpenAI, and Groq stay on the existing `ChatProvider` pattern.
The `langchain-anthropic` `ChatAnthropic` binding is used for Anthropic to produce tool-call
messages in the format ToolNode expects. `ToolNode` is always constructed with
`handle_tool_errors=True` (default in prebuilt 1.0.1 is `False` — GitHub Issue #6486).

## Consequences
- XML/JSON envelope parser is retired for the Anthropic path; remains active for other paths
- `ScriptedProvider` and all 208 existing tests are unaffected (ScriptedProvider is not the Anthropic path)
- `seen_tool_signatures` deduplication must be preserved as a pre-check before ToolNode executes
- OpenAI and Groq ToolNode migration deferred to a future phase
- One migration at a time reduces debugging surface
```

**ADR-003-annotated-reducers.md:**
```markdown
# ADR-003: Annotated[list[T], operator.add] Reducers for RunState List Fields

**Status:** Accepted
**Date:** 2026-03-02

## Context
`RunState` has four plain `list[T]` fields: `tool_history`, `mission_reports`, `memo_events`,
`seen_tool_signatures`. In LangGraph, when parallel branches (via `Send()`) converge, the
default merge behavior for non-annotated fields is last-writer-wins — meaning one branch's
records silently overwrite another's. Phase 4 will introduce parallel mission fan-out via
`Send()`. If reducers are not in place before then, the data loss will be silent and hard to diagnose.

## Decision
Add `Annotated[list[T], operator.add]` to all four list fields in `RunState`. This tells
LangGraph to concatenate (not replace) when parallel branches merge. `ensure_state_defaults()`
is NOT removed — it remains the repair function for missing keys, independent of reducer
annotations. All graph.py mutations of these fields are in-place appends (`state[field].append(...)`)
which do not go through the reducer; only dict-key returns go through it.

## Consequences
- Parallel Send() branches can be safely introduced in Phase 4 without data loss
- mypy may report warnings on `Annotated[list[T], operator.add]` because `operator.add` is
  `Callable`, not a type annotation — suppressed with `# type: ignore[misc]` if needed
- Node return values that include these fields as dict keys must return only the delta (new records),
  not the full accumulated list — audit confirms only one such occurrence in graph.py (line 227,
  in the run() result extraction, not a graph node return)
- All 208 existing sequential tests pass unchanged
```

**ADR-004-message-compaction.md:**
```markdown
# ADR-004: Sliding-Window Message Compaction (No LLM Summarization)

**Status:** Accepted
**Date:** 2026-03-02

## Context
Phase 3 will delegate to specialist subgraphs. Before delegation, the parent graph's
`messages` list may contain 20–50+ messages from prior planning/execution turns. Without
compaction, specialist subgraphs receive bloated context that may exceed provider context
windows and increase latency.

## Decision
Implement sliding-window compaction in `ensure_state_defaults()`: when `messages` exceeds
`P1_MESSAGE_COMPACTION_THRESHOLD` (default 40), drop the oldest non-system messages to
bring the list back to the threshold. System messages are always preserved. Compaction fires
automatically at every node entry because `ensure_state_defaults()` is called at node entry.

LLM summarization was explicitly rejected: it adds latency and introduces a live-LLM
dependency in what should be deterministic state management.

## Consequences
- Context window overflow is prevented before specialist delegation in Phase 3
- System prompt is never lost (system messages are separated before the sliding window is applied)
- Threshold is configurable via `P1_MESSAGE_COMPACTION_THRESHOLD` env var (useful for testing)
- Historical context beyond the window is lost — this is acceptable for the current single-agent
  workload; revisit if long-running sessions require more context depth
- No new dependencies required (stdlib `os.getenv` only)
```

After creating all files, verify the docs/ADR/ directory:
```
ls docs/ADR/
```
  </action>
  <verify>
    <automated>ls /home/nir/dev/agent_phase0/docs/ADR/</automated>
  </verify>
  <done>Four ADR files exist in docs/ADR/. Each contains all four mandatory sections (Status, Context, Decision, Consequences).</done>
</task>

</tasks>

<verification>
1. `grep "@observe" src/agentic_workflows/orchestration/langgraph/run.py` — returns a match on the run() function
2. `ls docs/ADR/` — shows 4 ADR files
3. `grep "## Context" docs/ADR/ADR-001-langgraph-version-upgrade.md` — matches
4. `pytest tests/ -q` — still 208+ tests passing
5. `ruff check src/ tests/` — exits 0
</verification>

<success_criteria>
`@observe(name="run")` is on the `run()` entrypoint in `run.py`. `docs/ADR/` contains 4 ADR files documenting the four key Phase 2 decisions. All tests pass. No new test failures from the decorator addition.
</success_criteria>

<output>
After completion, create `.planning/phases/02-langgraph-upgrade-and-single-agent-hardening/02-04-SUMMARY.md` with:
- Confirmation @observe() is wired and what span name it uses
- List of ADR files created with one-line summary of each decision
- Confirmation tests still pass
</output>
