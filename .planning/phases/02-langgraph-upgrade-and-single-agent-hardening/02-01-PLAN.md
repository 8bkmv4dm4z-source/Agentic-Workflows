---
phase: 02-langgraph-upgrade-and-single-agent-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: true
requirements:
  - LGUP-01

must_haves:
  truths:
    - "Developer can install from pyproject.toml with langgraph>=1.0.6 without dependency conflicts"
    - "All 208 existing tests pass unchanged after the version upgrade"
    - "ruff check and mypy pass after the upgrade"
  artifacts:
    - path: "pyproject.toml"
      provides: "Updated version constraints removing <1.0 pin"
      contains: "langgraph>=1.0.6"
  key_links:
    - from: "pyproject.toml"
      to: "langgraph==1.0.6"
      via: "pip install -e .[dev]"
      pattern: "langgraph>=1.0.6,<2.0"
    - from: "langgraph-prebuilt"
      to: "1.0.1 pin"
      via: "pyproject.toml constraint"
      pattern: "langgraph-prebuilt>=1.0.1,<1.0.2"
---

<objective>
Remove the `langgraph<1.0` version pin in `pyproject.toml` and upgrade to `langgraph==1.0.6` + `langgraph-prebuilt==1.0.1`. Verify all 208 existing tests still pass without any code changes.

Purpose: This is the master blocker for the entire Phase 2 roadmap. Until the pin is removed, ToolNode, tools_condition, and Annotated reducers are unavailable. All subsequent plans in this phase depend on this completing cleanly.

Output: `pyproject.toml` with updated constraints; confirmed 208-test green suite; `langchain-anthropic` added to dependencies for the ToolNode path (Plan 03 will wire it).
</objective>

<execution_context>
@/home/nir/.claude/get-shit-done/workflows/execute-plan.md
@/home/nir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-langgraph-upgrade-and-single-agent-hardening/02-CONTEXT.md
@.planning/phases/02-langgraph-upgrade-and-single-agent-hardening/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Upgrade langgraph version pin in pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
Edit `pyproject.toml` dependencies section. Make ONLY version constraint changes — no other edits.

Replace:
```
"langgraph>=0.2.67,<1.0",
```

With:
```
"langgraph>=1.0.6,<2.0",
"langgraph-prebuilt>=1.0.1,<1.0.2",
"langchain-anthropic>=0.3.0",
```

Rationale for each pin:
- `langgraph>=1.0.6,<2.0`: Stable 1.0.x series; <2.0 guards against next major break
- `langgraph-prebuilt>=1.0.1,<1.0.2`: Pin to 1.0.1 exactly because 1.0.2 introduced a ToolNode.afunc signature break (GitHub Issue #6363); move to a higher version only after confirming the fix landed
- `langchain-anthropic>=0.3.0`: Required by Plan 03 for ToolNode with Anthropic tool-call format; adding now so the install is complete before wiring begins

Do NOT change any other lines in pyproject.toml. Do NOT change dev extras or other dependency versions.

Then run: `pip install -e ".[dev]"` to install the updated dependencies.
  </action>
  <verify>
    <automated>pip show langgraph | grep -E "^Version:" && pip show langgraph-prebuilt | grep -E "^Version:" && pip show langchain-anthropic | grep -E "^Version:"</automated>
  </verify>
  <done>langgraph 1.0.6+, langgraph-prebuilt 1.0.1, langchain-anthropic installed and importable</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify full test suite green with new langgraph version</name>
  <files></files>
  <action>
Run the full test suite to confirm the langgraph 1.0.x upgrade is backwards-compatible with all 208 existing tests. No code changes are permitted in this task — if any test fails, diagnose the root cause before proceeding.

Command to run:
```
cd /home/nir/dev/agent_phase0 && pytest tests/ -q
```

Expected outcome: all 208 tests pass. The langgraph 1.0 series is documented as backwards-compatible with the 0.2.x API surface used in this codebase.

If a test fails, look for:
1. Import errors from removed 1.0 APIs (check langgraph changelog for removed symbols)
2. Behavior changes in graph compilation (check StateGraph constructor signature)
3. Recursion limit changes (langgraph 1.0 may have different defaults — check against max_steps × 3 convention)

Do NOT raise max_steps to fix recursion errors. Diagnose root cause.

If tests fail and cannot be fixed with a small targeted fix, STOP and report the specific failure before proceeding. Do not proceed to subsequent plans until 208 tests pass.

Also run quality gates:
```
ruff check src/ tests/
mypy src/
```
  </action>
  <verify>
    <automated>cd /home/nir/dev/agent_phase0 && pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest reports 208 tests passed, ruff clean, mypy clean (or mypy passes with no new errors introduced by the dependency bump)</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pip show langgraph` shows version 1.0.6 or higher (but below 2.0)
2. `pip show langgraph-prebuilt` shows version 1.0.1
3. `pip show langchain-anthropic` shows version 0.3.0 or higher
4. `pytest tests/ -q` reports 208 passed with 0 failures
5. `ruff check src/ tests/` exits 0
6. `cat pyproject.toml` contains `"langgraph>=1.0.6,<2.0"` and NOT `"langgraph>=0.2.67,<1.0"`
</verification>

<success_criteria>
The langgraph<1.0 pin is removed. langgraph 1.0.6, langgraph-prebuilt 1.0.1, and langchain-anthropic are installed. All 208 existing tests pass. This plan produces no behavioral code changes — only the version pin change.
</success_criteria>

<output>
After completion, create `.planning/phases/02-langgraph-upgrade-and-single-agent-hardening/02-01-SUMMARY.md` with:
- What changed (exact pyproject.toml diff)
- Installed versions confirmed
- Test results (208 pass / any unexpected findings)
- Any API surface changes noticed during the upgrade that subsequent plans should know about
</output>
