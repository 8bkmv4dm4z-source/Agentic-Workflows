---
phase: 07.7
slug: hybrid-intent-classifier-few-shot-prompts
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-10
---

# Phase 07.7 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | pyproject.toml `[tool.pytest.ini_options]` |
| **Quick run command** | `pytest tests/unit/ -q -x` |
| **Full suite command** | `pytest tests/ -q` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/unit/ -q -x`
- **After every plan wave:** Run `pytest tests/ -q`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Wave 0 Strategy

All plans use `tdd="true"` task types, which serve as Wave 0 equivalents: tests are written before production code within each task's RED-GREEN-REFACTOR cycle. No separate Wave 0 plan is needed.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | TDD Task | Status |
|---------|------|------|-------------|-----------|-------------------|----------|--------|
| 07.7-01-01 | 01 | 1 | SC-7 | unit | `pytest tests/unit/test_tool_contracts.py -x -k args_schema` | yes | pending |
| 07.7-01-02 | 01 | 1 | SC-7 | unit | `pytest tests/unit/test_tool_contracts.py -x -k args_schema` | no | pending |
| 07.7-01-03 | 01 | 1 | SC-7 | unit+regression | `pytest tests/ -x -q` | no | pending |
| 07.7-02-01 | 02 | 1 | SC-1 | unit | `pytest tests/unit/test_mission_parser.py -x -k intent` | yes | pending |
| 07.7-03-01 | 03 | 2 | SC-2 | unit | `pytest tests/unit/test_prompt_tier.py -x -k few_shot` | yes | pending |
| 07.7-03-02 | 03 | 2 | SC-2,SC-3 | unit | `pytest tests/unit/test_prompt_tier.py -x -k "budget or few_shot or directive_section"` | yes | pending |
| 07.7-04-01 | 04 | 2 | SC-4 | integration | `pytest tests/unit/test_model_router_wiring.py -x -k intent` | yes | pending |
| 07.7-04-02 | 04 | 2 | SC-6 | unit | `pytest tests/unit/test_action_parser.py -x -k escalation` | yes | pending |
| regression | all | all | SC-5 | regression | `pytest tests/ -q` | n/a | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

All test files are created within their respective TDD tasks (tests written first, then production code). No separate Wave 0 plan needed:

- `tests/unit/test_tool_contracts.py` — created in Plan 01 Task 1 (tdd=true)
- `tests/unit/test_mission_parser.py` — extended in Plan 02 Task 1 (tdd=true)
- `tests/unit/test_prompt_tier.py` — created in Plan 03 Task 1 (tdd=true)
- `tests/unit/test_model_router_wiring.py` — created in Plan 04 Task 1 (tdd=true)
- `tests/unit/test_action_parser.py` — created in Plan 04 Task 2 (tdd=true)

*Existing infrastructure covers framework and fixtures — only test files need new cases.*

---

## Phase-Level Regression Gate

SC-5 ("All existing tests pass unchanged") is a phase-level regression gate, not specific to any single plan. Every plan's final task must run `pytest tests/ -x -q` and confirm no regressions. This is verified in each plan's `<verification>` section.

---

## Manual-Only Verifications

*All phase behaviors have automated verification.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or TDD task types (Wave 0 equivalent)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covered by TDD task types — tests created before production code
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved
