---
phase: 07.7-hybrid-intent-classifier-few-shot-prompts
plan: 01
subsystem: tools
tags: [args-schema, tool-registry, json-schema, compact-prompt]

# Dependency graph
requires:
  - phase: 07.2
    provides: "tool contract test scaffold"
provides:
  - "Tool.args_schema property on all 37+ tools"
  - "Typed JSON schema generation via _build_action_json_schema()"
  - "Accurate compact prompt arg signatures via _tool_sig()"
affects: [07.7-02, 07.7-03, 07.7-04]

# Tech tracking
tech-stack:
  added: []
  patterns: ["_args_schema class variable override pattern on Tool subclasses"]

key-files:
  created: []
  modified:
    - src/agentic_workflows/tools/base.py
    - src/agentic_workflows/orchestration/langgraph/graph.py
    - tests/unit/test_tool_contracts.py

key-decisions:
  - "args_schema property with _args_schema class var override; falls back to required_args() regex for backward compat"
  - "_tool_sig shows ALL args (not just required) so LLM sees full parameter set"
  - "Schema uses string 'required': 'true' convention to mark mandatory args"

patterns-established:
  - "_args_schema class variable: every Tool subclass declares its arg types as dict[str, dict[str, str]]"
  - "args_schema property: read _args_schema if set, else fall back to required_args() regex"

requirements-completed: [SC-7]

# Metrics
duration: 4min
completed: 2026-03-10
---

# Phase 07.7 Plan 01: Tool Args Schema Summary

**Typed args_schema property on all 37 tools replacing fragile regex-based required_args() for JSON schema and compact prompt generation**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-10T13:42:46Z
- **Completed:** 2026-03-10T13:47:21Z
- **Tasks:** 3
- **Files modified:** 40

## Accomplishments
- Added `_args_schema` class variable and `args_schema` property to Tool base class with backward-compatible fallback
- Migrated all 37 registered tool subclasses with typed argument schemas (type, required status)
- Updated `_build_action_json_schema()` to generate tool-specific arg schemas with correct types (array, number, object, boolean)
- Updated `_tool_sig()` to show all args in compact prompt signatures (e.g., `write_file(path, content)`)
- Added 75 parametrized args_schema contract tests, all passing alongside 1428 total tests

## Task Commits

Each task was committed atomically:

1. **Task 1: Add args_schema to Tool base + migrate first 18 tool subclasses** - `f29444c` (feat)
2. **Task 2: Migrate remaining 20 tool subclasses to _args_schema** - `9f3d449` (feat)
3. **Task 3: Update _build_action_json_schema and _tool_sig to use args_schema** - `b7a344e` (feat)

## Files Created/Modified
- `src/agentic_workflows/tools/base.py` - Added _args_schema class var + args_schema property
- `src/agentic_workflows/orchestration/langgraph/graph.py` - Updated _build_action_json_schema() and _tool_sig()
- `tests/unit/test_tool_contracts.py` - Added 3 test functions (75 parametrized cases)
- 37 tool files - Added _args_schema class variable with typed arg definitions

## Decisions Made
- args_schema property falls back to required_args() regex parsing for any tool without _args_schema set (backward compat)
- _tool_sig() shows ALL args (required + optional) in compact prompt so LLM knows full parameter set
- Used "required": "true" string convention in schema dicts (not boolean) to keep dict[str, dict[str, str]] typing simple

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All tools now have typed schemas ready for few-shot prompt generation (Plan 02)
- args_schema available for any future tool validation or documentation generation

---
*Phase: 07.7-hybrid-intent-classifier-few-shot-prompts*
*Completed: 2026-03-10*

## Self-Check: PASSED
