---
phase: 07.7-hybrid-intent-classifier-few-shot-prompts
plan: 02
subsystem: orchestration
tags: [intent-classification, mission-parser, model-routing, llm-timeout]

requires:
  - phase: 07.7-01
    provides: Tool._args_schema for structured tool metadata
provides:
  - IntentClassification dataclass with to_dict/from_dict
  - _deterministic_classify() heuristic classifier
  - _classify_intent() LLM classifier with 500ms timeout fallback
  - parse_missions() classifier_provider and classifier_timeout params
  - StructuredPlan.intent_classification field with serialization
affects: [07.7-04-model-router-wiring]

tech-stack:
  added: []
  patterns: [threaded-llm-with-deterministic-fallback, TYPE_CHECKING-import-guard]

key-files:
  created: []
  modified:
    - src/agentic_workflows/orchestration/langgraph/mission_parser.py
    - tests/unit/test_mission_parser.py

key-decisions:
  - "TYPE_CHECKING import guard for ChatProvider to avoid circular imports"
  - "_COMPLEX_TOOLS set defined at module level for deterministic classification"
  - "Threading + queue pattern reused from existing parse_missions timeout for LLM classifier"
  - "response_schema param passed to provider.generate() for structured output"

patterns-established:
  - "LLM-with-deterministic-fallback: try LLM in thread with timeout, fall back to heuristic on failure"

requirements-completed: [SC-1]

duration: 4min
completed: 2026-03-10
---

# Phase 07.7 Plan 02: Intent Classification Summary

**IntentClassification dataclass with LLM+deterministic hybrid classifier, 500ms timeout, and StructuredPlan serialization**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-10T13:42:52Z
- **Completed:** 2026-03-10T13:47:01Z
- **Tasks:** 1 (TDD: RED + GREEN)
- **Files modified:** 2

## Accomplishments
- IntentClassification dataclass with complexity/mission_type/confidence/source fields
- Deterministic classifier using step count, tool complexity, and parsing method heuristics
- LLM classifier with threading + queue timeout pattern (same as parse_missions)
- StructuredPlan serialization handles intent_classification (including None for old data)
- 9 new intent classification tests, all 22 mission_parser tests pass

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED): Failing intent tests** - `c0a1086` (test)
2. **Task 1 (GREEN): Intent classification implementation** - `0b70c95` (feat)

_TDD task with RED/GREEN commits._

## Files Created/Modified
- `src/agentic_workflows/orchestration/langgraph/mission_parser.py` - IntentClassification dataclass, _deterministic_classify(), _classify_intent(), _apply_intent_classification(), updated parse_missions() signature and StructuredPlan
- `tests/unit/test_mission_parser.py` - 9 new tests covering deterministic, LLM, timeout, serialization, and backward compatibility

## Decisions Made
- TYPE_CHECKING import guard for ChatProvider avoids circular import with provider.py
- _COMPLEX_TOOLS set at module level: data_analysis, outline_code, read_file_chunk, run_bash, http_request
- Reused threading + queue pattern from existing parse_missions timeout for consistency
- response_schema passed to provider.generate() for structured output when available
- Deterministic confidence=0.6/0.7, LLM confidence=0.8 for downstream routing decisions

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

Pre-existing test_tool_contracts flaky failure (sort_array/write_file args_schema) observed during full suite run -- not related to changes. Out of scope.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- IntentClassification available in StructuredPlan for ModelRouter wiring (Plan 04)
- parse_missions() backwards-compatible -- classifier_provider defaults to None (deterministic only)

---
*Phase: 07.7-hybrid-intent-classifier-few-shot-prompts*
*Completed: 2026-03-10*
